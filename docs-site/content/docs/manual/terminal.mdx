---
title: "Move around windows, tabs, panes and sessions"
description: "WezTerm's jump mode and the session commands."
---

{/* GENERATED by scripts/generate-manual.mjs — edit the .nuon surface, not this file. */}

## `q / :q / /exit`

**Leave the shell**

*Shell*

Any of the three exits. They exist because muscle memory arrives from vim, from a REPL, and from a chat box, and none of them should print an error.

Spec: `prds/04-shell/02-aliases-utilities/prd.md`

## `F5 <digit>`

**Jump to a window by its number**

*Terminal*

Press `F5`, let go, then a digit 1-9: you are on that window. If there is no window with that number yet, it is created there and you land in it at `~`. The mode is over the moment you press the second key — the next thing you type goes into the pane. Any unbound key cancels.

<Callout title="Why it is this way">
A digit is always a valid address, and that is the whole design. tmux window indices do not renumber, so killing window 4 leaves a hole — the digit creates the window rather than doing nothing, which is why nothing has to be kept resident to hold the address open. A key with no binding in the pushed table is looked up once more in the root table and dropped if it misses, so a mistyped letter ends the mode and types nothing, with no bindings needed to arrange it. A lazily created window starts at `~` and NOT at the current directory — that disagrees with `F4` on purpose: a digit is a clean slate, a split is a division of the work in front of you.
</Callout>

See also: [`F5 <letter>`](/docs/manual/terminal#f5-letter) · [`F4 <arrow>`](/docs/manual/terminal#f4-arrow) · [`F5 F5`](/docs/manual/terminal#f5-f5)  
Spec: `prds/07-multiplexer/02-key-tables/prd.md`

## `F5 <letter>`

**Jump to a pane by its letter**

*Terminal*

Press `F5`, let go, then `a` to `i`: focus moves to that pane. The letters are printed on the pane borders whenever a window has more than one pane, so the label you are reading is the key you press.

<Callout title="Why it is this way">
The letters address the pane INDEX, and tmux renumbers panes when one is killed — so a letter does not keep its pane for life. That is exactly why the border prints it: the label is derived from the same index the key uses, so it is right the instant after a renumber rather than describing where a pane used to be. The two halves are one design and neither is honest without the other. This is the capability WezTerm refused: `PaneSelect` cannot be closed from Lua, so letter addressing there would have meant painting an overlay by hand — nine `select-pane -t` lines do it here.
</Callout>

See also: [`F5 <digit>`](/docs/manual/terminal#f5-digit) · [`F4 <arrow>`](/docs/manual/terminal#f4-arrow) · [`the status bar`](/docs/manual/terminal#the-status-bar)  
Spec: `prds/07-multiplexer/02-key-tables/prd.md`

## `F4 <arrow>`

**Split the view in a direction**

*Terminal*

Press `F4`, let go, then an arrow: the pane splits that way and the new pane opens in the directory the old one was in. `Left` and `Up` put the new pane before the current one, `Right` and `Down` after it.

<Callout title="Why it is this way">
A split inherits the current directory because you are dividing the work in front of you; a lazily created window starts at `~` because a digit is a clean slate. The two gestures disagree on purpose. The directory comes from the shell's OSC 7 report, which is why `config.nu` keeps `osc7: true` — its neighbour `osc133` stays OFF for an unrelated and expensive reason: double-marking left phantom prompt lines.
</Callout>

See also: [`F5 <letter>`](/docs/manual/terminal#f5-letter) · [`F5 <digit>`](/docs/manual/terminal#f5-digit)  
Spec: `prds/07-multiplexer/02-key-tables/prd.md`

## `F5 F5`

**Send F5 or F4 through to a nested session**

*Terminal*

Press the key twice — `F5 F5`, or `F4 F4` — and the second press is sent to whatever is running in the pane instead of being handled here. That is how you reach the window and split keys of a tmux you have ssh'd into: the outer session gets the first press, the inner one gets the second.

<Callout title="Why it is this way">
These keys are bound without a prefix, so the outermost session eats them always — which is the portability case biting itself the moment you ssh somewhere that also runs tmux. The double tap needs an explicit binding and does not fall out for free: without it the second press misses in the pushed table, is looked up again in the root table, finds the same key there and silently re-arms the mode. `F6` is deliberately NOT forwardable: the palette belongs to the outermost terminal, because that is the process that owns the colours and reads the escape sequence.
</Callout>

See also: [`F5 <digit>`](/docs/manual/terminal#f5-digit) · [`F4 <arrow>`](/docs/manual/terminal#f4-arrow) · [`F6`](/docs/manual/config#f6)  
Spec: `prds/07-multiplexer/02-key-tables/prd.md`

## `the status bar`

**Read the bar across the top**

*Terminal*

Nothing to press. The window numbers run along the left of the bar: the one you are in has a filled background, and a number shows lit rather than dimmed when something is running in any of that window's panes — one glance says where the work is. On the right is the current pane's directory and the clock. Over ssh the machine's name appears on the far left; on this machine that space is blank, because you already know.

<Callout title="Why it is this way">
Background says which window has focus, foreground says which windows are busy — two signals that never have to be decoded out of one channel. A window counts as busy when any of its panes is running something that is not a shell, read from the pane's foreground process rather than from prompt marking, which is deliberately off. There is no key legend on the bar: the digits are the legend, and a legend in words was tried and removed as noise. The colours are ANSI slots, never fixed values, so a theme switch retints the bar with everything else.
</Callout>

See also: [`F5 <digit>`](/docs/manual/terminal#f5-digit) · [`F5 <letter>`](/docs/manual/terminal#f5-letter)  
Spec: `prds/07-multiplexer/03-status-bar/prd.md`

## `the session survives`

**Close the terminal without losing your work**

*Terminal*

Quit the terminal — or lose the ssh connection — and nothing stops: reopening it puts you back exactly where you were, same windows, same panes, same directories. A reboot costs you at most fifteen minutes of layout changes: the arrangement is saved on a timer and restored when the machine comes back, with editors reopening on the files they had.

<Callout title="Why it is this way">
The session outliving the terminal is free — the server is a separate process and the attach is idempotent, so opening the terminal again is the same gesture as opening it the first time. Surviving a REBOOT is not free and is two plugins: one saves and restores the layout, the other runs the timer. What comes back is the shape of the work — where each pane was, what it was running — and not a copy of its scrollback, which would put the output of every command on disk and grow without bound. Editors come back through the editor's own session file rather than by dropping an untracked file in every project directory.
</Callout>

See also: [`the status bar`](/docs/manual/terminal#the-status-bar) · [`F5 <digit>`](/docs/manual/terminal#f5-digit)  
Spec: `prds/07-multiplexer/07-persistence/prd.md`

## `Ctrl+Shift+X`

**Freeze the screen and select text with the keyboard**

*Terminal*

Press `Ctrl+Shift+X` and the pane stops scrolling: you get a cursor that moves on vi keys — `hjkl` to move, `w` and `b` by word, `g` and `G` to the ends of the scrollback, `/` to search it. Press `c` where the selection should start; each further `c` widens it from the character to the word to the whole line and then back to the character. `y` copies what is selected and leaves; `q` or `Esc` leave with nothing copied.

<Callout title="Why it is this way">
The gesture is carried over from the terminal's own copy mode; the motions underneath are tmux's, which is why searching the scrollback is `/` from inside the mode now rather than a separate key from outside it. Leaving copy mode by ANY route clears the widen cycle, through a hook rather than through the exit keys — copy mode is also entered by the mouse wheel and by the `copymode` command, and a cycle left half-armed for the next time was the failure the hook exists to prevent. Where the copy lands is decided once, when the config loads: the local clipboard where there is one, and an escape sequence to the terminal where there is not — which is what makes copying work from a session at the far end of an ssh.
</Callout>

See also: [`Ctrl+C`](/docs/manual/terminal#ctrl-c) · [`Ctrl+V`](/docs/manual/terminal#ctrl-v) · [`copymode`](/docs/manual/terminal#copymode)  
Spec: `prds/07-multiplexer/05-copy-and-clipboard/prd.md`

## `copymode`

**Enter copy mode without touching a chord**

*Shell*

Type `copymode` at a host prompt and the pane it runs in freezes into copy mode, exactly as `Ctrl+Shift+X` leaves it — the same motions, the same widening `c` cycle. Outside tmux it says so rather than doing nothing.

<Callout title="Why it is this way">
One command addressed at the pane it was typed in. It used to print an escape sequence the terminal parsed off the pty, because that was the only route a shell command had into a GUI-only mode; the mode is the multiplexer's now and the multiplexer has a command for it, so the trick is gone.
</Callout>

See also: [`Ctrl+Shift+X`](/docs/manual/terminal#ctrl-shift-x)  
Spec: `prds/07-multiplexer/05-copy-and-clipboard/prd.md`

## `Ctrl+V`

**Paste the clipboard into whatever is running**

*Terminal*

Press `Ctrl+V` in any pane — a shell prompt, nvim, a running agent alike. The clipboard lands in one piece: nothing to confirm, and no reindenting in the editor.

<Callout title="Why it is this way">
Bracketed paste is the mechanism: the text arrives wrapped in the escape sequence that marks it as pasted rather than typed, so a program that would reindent or autocomplete every keystroke can tell the two apart. That marker is also the only route clipboard-based dictation has into a terminal. Still the terminal's binding rather than the multiplexer's, because it is about this machine's clipboard.
</Callout>

See also: [`Ctrl+C`](/docs/manual/terminal#ctrl-c)  
Spec: `prds/02-terminal/04-copy-mode/prd.md`

## `Ctrl+C`

**Copy the selection, or interrupt when there is none**

*Terminal*

With text selected by the mouse, `Ctrl+C` copies it and drops the selection. With nothing selected the same key is the ordinary interrupt and reaches the program as SIGINT, so you never spend a stop on a copy that was not there.

<Callout title="Why it is this way">
The binding reads the pane's selection first and only falls through to sending the key when it is empty — that is what buys the platform-native copy shortcut without giving up the interrupt. It stays meaningful because MOUSE selection is deliberately left to the terminal: the multiplexer's mouse mode is off, so dragging still selects the way it does in any other window.
</Callout>

See also: [`Ctrl+V`](/docs/manual/terminal#ctrl-v) · [`Ctrl+Shift+X`](/docs/manual/terminal#ctrl-shift-x)  
Spec: `prds/02-terminal/04-copy-mode/prd.md`

## `Ctrl+Alt+Super+drag`

**Move the window without a titlebar**

*Terminal*

Hold all three of `Ctrl`, `Alt` and `Cmd`, then drag anywhere in the window with the left button and the whole OS window follows. An ordinary drag still selects text.

<Callout title="Why it is this way">
`RESIZE` decorations leave no titlebar to grab, so this is the only handle for repositioning the window. The deliberately heavy three-modifier combo is what keeps it from stealing plain clicks and selection drags.
</Callout>

See also: [`Ctrl+click`](/docs/manual/terminal#ctrl-click)  
Spec: `prds/02-terminal/04-copy-mode/prd.md`

## `Ctrl+click`

**Open the link under the cursor**

*Terminal*

Hold `Ctrl` and left-click a URL and it opens in the browser, even while nvim or another full-screen TUI owns the mouse. A plain click still reaches the program.

<Callout title="Why it is this way">
The binding sets `mouse_reporting`, which is what keeps it working while an application is capturing the mouse. Without that flag the click is forwarded to the application and nobody opens the URL.
</Callout>

See also: [`Ctrl+Alt+Super+drag`](/docs/manual/terminal#ctrl-alt-super-drag)  
Spec: `prds/02-terminal/04-copy-mode/prd.md`
