---
title: "Edit text, on the command line and in Neovim"
description: "The command-line keys, the Neovim keymaps, and the editor-style selection."
---

{/* GENERATED by scripts/generate-manual.mjs — edit the .nuon surface, not this file. */}

## `Esc`

**Clear the command line**

*Shell*

Press `Esc` and the command line clears, the way `Ctrl-C` would. It is an unconditional clear with no menu-close branch, so the line goes whether or not a completion or history menu is open — this is not a two-stage escape.

Spec: `prds/04-shell/01-core-config/prd.md`

## `cat <file>`

**Dump a file with syntax highlighting and no pager**

*Shell*

`cat <file>` is `bat --paging=never`: highlighted, line-numbered, and it still pipes like `cat` because nothing waits for a key.

Spec: `prds/04-shell/02-aliases-utilities/prd.md`

## `nv / vi`

**Open Neovim**

*Shell*

Either name starts nvim. `$EDITOR` and `$VISUAL` are nvim too, so anything that opens an editor for you — the finder, `z` on a file, a commit message — lands in the same place.

See also: [`nn`](/docs/manual/edit#nn)  
Spec: `prds/04-shell/02-aliases-utilities/prd.md`

## `nn`

**Open the notes file**

*Shell*

`nn` opens `~/notes.md` in nvim, from wherever you are.

See also: [`nv / vi`](/docs/manual/edit#nv-vi)  
Spec: `prds/04-shell/02-aliases-utilities/prd.md`

## `cf <file>`

**Copy a file's contents to the system clipboard**

*Shell*

`cf <file>` puts the whole file on the clipboard. The argument is `path`-typed, so Tab completes it. A missing file errors instead of copying nothing.

<Callout title="Why it is this way">
It picks `pbcopy`, `wl-copy` or `xclip` by session type and checks the display variables first, so on a headless box it fails fast rather than hanging on a clipboard nobody is listening to.
</Callout>

Spec: `prds/04-shell/02-aliases-utilities/prd.md`

## `<leader>`

**Show what the leader key can do**

*Neovim — normal mode*

Press space and pause: which-key lists the groups — `f` find, `b` buffer, `c` code, `r` rename/refactor, `t` table — and then the maps inside whichever one you press.

<Callout title="Why it is this way">
Leader is space, and it is set before any plugin spec is evaluated: a spec that declares `keys = { "<leader>x" }` resolves leader at declaration time, so setting it later silently binds the wrong key.
</Callout>

Spec: `prds/03-editor/12-small-plugins/prd.md`

## `<Esc>`

**Clear highlight**

*Neovim — normal mode*

Press `Esc` in normal mode to run `:nohlsearch`. Nothing visible happens in this config: `hlsearch` is off, so matches never stay highlighted and there is nothing to drop. Turn the option on and this is the key that clears it; the search itself is untouched either way, so `n` still works.

<Callout title="Why it is this way">
The map and the option have to be decided together or the key is dead, and live bug L-6 decided it the other way round: keep `hlsearch = false` and drop the map. So this documents a binding the rebuild is not meant to carry over.
</Callout>

Spec: `prds/03-editor/02-keymaps/prd.md`

## `q`

**Close a utility buffer**

*Neovim — normal mode*

Press `q` in a help, quickfix, man, lspinfo, checkhealth or startuptime buffer to close it — one key across all six. The buffer is also marked unlisted the moment it opens, so it never turns up under `<S-h>`/`<S-l>` or `:bnext` either.

Spec: `prds/03-editor/03-autocmds/prd.md`

## `<C-h> <C-j> <C-k> <C-l>`

**Move between windows**

*Neovim — normal mode*

Control plus a vim direction letter moves the cursor to the window that way — no `<C-w>` prefix.

See also: [`<C-Up> <C-Down> <C-Left> <C-Right>`](/docs/manual/edit#c-up-c-down-c-left-c-right) · [`<leader>| and <leader>-`](/docs/manual/edit#leader-and-leader)  
Spec: `prds/03-editor/02-keymaps/prd.md`

## `<C-Up> <C-Down> <C-Left> <C-Right>`

**Resize the current window**

*Neovim — normal mode*

Control plus an arrow resizes by two: up and down change height, left and right change width. Hold it to keep going.

See also: [`<C-h> <C-j> <C-k> <C-l>`](/docs/manual/edit#c-h-c-j-c-k-c-l)  
Spec: `prds/03-editor/02-keymaps/prd.md`

## `<leader>| and <leader>-`

**Split the window**

*Neovim — normal mode*

`<leader>|` splits vertically (the new window is to the right), `<leader>-` splits horizontally (the new window is below). The bar and the dash look like the split they make.

See also: [`<C-h> <C-j> <C-k> <C-l>`](/docs/manual/edit#c-h-c-j-c-k-c-l)  
Spec: `prds/03-editor/02-keymaps/prd.md`

## `<S-h> <S-l> <leader>bd`

**Walk and close buffers**

*Neovim — normal mode*

`H` and `L` step to the previous and next buffer; `<leader>bd` runs a plain `:bdelete` on the one you are in. The buffer leaves the list — and so does its window: this is the bare command, so a split showing that buffer closes with it rather than falling back to another buffer.

See also: [`<leader>fb`](/docs/manual/find#leader-fb)  
Spec: `prds/03-editor/02-keymaps/prd.md`

## `<A-j> <A-k>`

**Move a line or a selection up and down**

*Neovim — normal mode*

Alt with `j` or `k` drags the current line. In visual mode it drags the whole selection, and the selection stays selected and re-indented for the next press, so you can walk a block through a file.

Spec: `prds/03-editor/02-keymaps/prd.md`

## `<C-d> <C-u> n N`

**Keep the cursor centred while jumping**

*Neovim — normal mode*

Half-page scrolls and search steps re-centre the line you land on, and `n`/`N` also open a fold you land inside. Nothing to press differently — the keys are the stock ones.

<Callout title="Why it is this way">
These four maps carry no description on purpose: which-key should not list a key whose behaviour is the vim default plus a re-centre, and the drift check treats a missing `desc` here as intended rather than as a mismatch.
</Callout>

Spec: `prds/03-editor/02-keymaps/prd.md`

## `< and > (visual)`

**Indent a selection without losing it**

*Neovim — visual mode*

`<` and `>` shift the selection and leave it selected, so you can press `>` four times instead of `4>` and reselecting.

Spec: `prds/03-editor/02-keymaps/prd.md`

## `<leader>w and <leader>q`

**Save and quit**

*Neovim — normal mode*

`<leader>w` writes the buffer, `<leader>q` quits the window.

Spec: `prds/03-editor/02-keymaps/prd.md`

## `<leader>p (visual)`

**Paste (keep register)**

*Neovim — visual mode*

Select, then `<leader>p`: the selection is replaced and the register still holds what you copied, so you can paste the same thing over several places in a row.

<Callout title="Why it is this way">
A plain `p` over a selection puts the deleted text into the unnamed register, which is why the second paste always pastes the wrong thing. This deletes into the black hole register first.
</Callout>

See also: [`<C-v> (visual)`](/docs/manual/edit#c-v-visual)  
Spec: `prds/03-editor/02-keymaps/prd.md`

## `<S-Up> <S-Down> <S-Left> <S-Right>`

**Select text with shift and the arrows**

*Neovim — normal mode*

Hold shift and press an arrow: from normal mode it starts a selection, from an existing selection it extends it, and from insert mode it leaves insert and selects — from insert, `<S-Left>` catches the last two characters rather than one. It behaves like every non-modal editor.

<Callout title="Why it is this way">
The insert cursor sits between characters and leaving insert drops it onto the one behind, which every shift map out of insert has to correct for — in opposite directions. `<S-Right>` moves right once before entering visual, or the character under the cursor is left out; leftward there is nothing to correct, and the extra character is the price.
</Callout>

See also: [`h j k l (visual)`](/docs/manual/edit#h-j-k-l-visual) · [`<C-c> (visual)`](/docs/manual/edit#c-c-visual)  
Spec: `prds/03-editor/14-shift-select/prd.md`

## `h j k l (visual)`

**Move (collapse selection)**

*Neovim — visual mode*

In a selection that shift started, a plain motion — `h`/`j`/`k`/`l` or an unshifted arrow — drops the selection and moves, the way it would in a normal editor. A count still applies: `3j` collapses and goes down three lines. In a selection you started with `v`, the same keys extend it, exactly as stock vim does.

<Callout title="Why it is this way">
This is the half most configs get wrong, and it needs a flag rather than a mapping: the flag records that the selection began with shift, and a `ModeChanged` autocmd clears it on leaving visual mode. Without that reset, the next `v` selection would inherit collapse-on-motion and stop behaving like vim.
</Callout>

See also: [`<S-Up> <S-Down> <S-Left> <S-Right>`](/docs/manual/edit#s-up-s-down-s-left-s-right)  
Spec: `prds/03-editor/14-shift-select/prd.md`

## `<C-c> (visual)`

**Copy to clipboard**

*Neovim — visual mode*

`<C-c>` on a selection yanks it and leaves visual mode. The clipboard is shared with the system, so it pastes into the terminal and into any other application.

<Callout title="Why it is this way">
`clipboard=unnamedplus` is what makes this work in both directions — there is no separate editor clipboard to copy out of.
</Callout>

See also: [`<C-v> (visual)`](/docs/manual/edit#c-v-visual) · [`<leader>p (visual)`](/docs/manual/edit#leader-p-visual)  
Spec: `prds/03-editor/14-shift-select/prd.md`

## `<C-v> (visual)`

**Paste over selection**

*Neovim — visual mode*

`<C-v>` on a selection replaces it with the clipboard, and the clipboard still holds what it held, so you can do it again somewhere else.

<Callout title="Why it is this way">
Note what this costs: `<C-v>` no longer starts visual-block from within a selection. Blockwise select is still `<C-v>` from normal mode.
</Callout>

See also: [`<C-c> (visual)`](/docs/manual/edit#c-c-visual) · [`<leader>p (visual)`](/docs/manual/edit#leader-p-visual)  
Spec: `prds/03-editor/14-shift-select/prd.md`

## `<Tab> <S-Tab> <C-n> <C-p> <C-Space> <C-e>`

**Drive the completion menu**

*Neovim — insert mode*

`<Tab>` selects and accepts, and jumps to the next snippet placeholder once you are inside one; `<S-Tab>` goes back. `<C-n>`/`<C-p>` cycle, `<C-Space>` toggles the menu, `<C-e>` hides it. Documentation for the selected item appears on its own after a moment, and signature help shows while you are inside a call.

<Callout title="Why it is this way">
`<CR>` accepts the selected item and `<Esc>` cancels the menu, but both fall back to their ordinary meaning when no menu is open — that fallback is load-bearing: without it you could not insert a newline or leave insert mode.
</Callout>

Spec: `prds/03-editor/05-completion/prd.md`

## `<BS> and <CR> (autopairs)`

**Delete a bracket pair, and let Enter fall through to it**

*Neovim — insert mode*

`<BS>` next to an auto-inserted closing bracket or quote deletes both characters as one press, the same key as anywhere else. `<CR>` right after an opening bracket splits it onto three lines with the cursor indented in between — `{` then Enter gives `{`, a blank indented line, `}` — but only when no completion menu is showing; with one open, `<CR>` accepts the highlighted item instead, exactly as the entry above describes.

<Callout title="Why it is this way">
Both keys look like they belong entirely to completion, and only one does. blink.cmp maps `<CR>` to `{"accept", "fallback"}`: `fallback` means "run whatever `<CR>` did before blink.cmp claimed it", and because autopairs sets up first in load order, that prior mapping is autopairs' own bracket-expand handler — so the expand survives, but only through blink.cmp's fallback chain, and only while no menu is open. `<BS>` has no such chain: it is the one map autopairs sets that nothing else claims, so its default wins outright.
</Callout>

Spec: `prds/03-editor/12-small-plugins/prd.md`

## `<leader>e`

**Open the current directory as a buffer**

*Neovim — normal mode*

Press `<leader>e` in any buffer: oil opens on the directory of the current file, dotfiles included. It is a normal buffer — rename a line, delete a line, add a line, then `:w` and the filesystem matches.

<Callout title="Why it is this way">
`<leader>e` is the fast path, not the only one: oil loads eagerly (`lazy = false`) so its `default_file_explorer` hijack is installed at startup, and `:e some/dir` reaches it with no key pressed. Netrw is disabled either way, so nothing else could claim the directory instead.
</Callout>

Spec: `prds/03-editor/06-explorer/prd.md`

## `<leader>cf`

**Format the buffer now**

*Neovim — normal mode*

Press `<leader>cf` and the buffer formats asynchronously. You rarely need it: saving formats already — stylua for lua, rustfmt for rust, black for python, prettier for markdown — and a filetype with no formatter of its own is formatted by its language server instead.

<Callout title="Why it is this way">
Prettier is the markdown formatter because it aligns table pipes, but prose must stay exactly as wrapped: these files are hand-wrapped at ~78 columns and reflowing them would churn every diff. On-save formatting gives up after 500 ms rather than blocking the write.
</Callout>

Spec: `prds/03-editor/07-formatting/prd.md`

## `<leader>t`

**Work with markdown tables**

*Neovim — normal mode*

In a markdown buffer, table mode is already on: type a row and the pipes realign as you type, with GitHub-style `|` corners. `<leader>t` is the prefix for the rest — `<leader>tt` tableizes the line or the selection, `<leader>tm` toggles the mode, `<leader>tr` realigns. Moving between cells is not under the prefix: `[|` and `]|` step left and right, `{|` and `}|` up and down.

<Callout title="Why it is this way">
Table mode is switched on by a FileType autocmd, not by the plugin itself: `vim-table-mode` ships inactive and needs `TableModeEnable` per buffer. Without the autocmd only the file you opened nvim with would align, and every markdown buffer you `:edit` afterwards would not.
</Callout>

See also: [`<leader>cf`](/docs/manual/edit#leader-cf)  
Spec: `prds/03-editor/15-markdown-tables/prd.md`

## `gd and gI`

**Jump to a definition or an implementation**

*Neovim — normal mode*

`gd` on a symbol goes to where it is defined, `gI` to where it is implemented. `<C-o>` comes back.

<Callout title="Why it is this way">
These two are aliases for keys Neovim already provides, kept because the fingers know them. They attach per buffer on `LspAttach`, so they only exist where a language server is running — introspecting them needs an attached buffer, not a bare `nvim --headless`.
</Callout>

See also: [`Neovim's own LSP keys`](/docs/manual/edit#neovim-s-own-lsp-keys) · [`<leader>rn and <leader>ca`](/docs/manual/edit#leader-rn-and-leader-ca)  
Spec: `prds/03-editor/09-lsp/prd.md`

## `<leader>rn and <leader>ca`

**Rename a symbol or take a code action**

*Neovim — normal mode*

`<leader>rn` renames across the project — type the new name, enter. `<leader>ca` offers the actions the server has for the thing under the cursor. Both are buffer-local, so they appear once a server attaches.

See also: [`Neovim's own LSP keys`](/docs/manual/edit#neovim-s-own-lsp-keys) · [`gd and gI`](/docs/manual/edit#gd-and-gi)  
Spec: `prds/03-editor/09-lsp/prd.md`

## `Neovim's own LSP keys`

**Use the LSP keys Neovim already ships**

*Neovim — normal mode*

`K` hovers. `grn` renames, `gra` acts, `grr` lists references, `gri` implementations, `gO` the symbols in the file. `]d` and `[d` walk diagnostics, which show inline with a `●`; to read one in full press `<C-w>d`, which is the key that opens the bordered float naming the server it came from.

<Callout title="Why it is this way">
Documented although they are not ours: Neovim provides them itself from 0.11 onward, this config deliberately does not re-map them, and they are how you use this editor. They are the one exception to the rule that plugin- and core-provided maps are allowlisted out of the drift check rather than documented.
</Callout>

See also: [`gd and gI`](/docs/manual/edit#gd-and-gi) · [`<leader>rn and <leader>ca`](/docs/manual/edit#leader-rn-and-leader-ca)  
Spec: `prds/03-editor/09-lsp/prd.md`

## `<leader>ss <leader>sl <leader>sd`

**Restore the buffers you had open here**

*Neovim — normal mode*

The session for a directory is written automatically when you quit Neovim with at least one real file open. `<leader>ss` restores the one belonging to the directory you are in, `<leader>sl` restores the most recent session from anywhere, and `<leader>sd` turns the save off for this run when you do not want what you are about to do remembered. Nothing is restored unless you ask: starting `nvim` plainly gives you an empty editor, as it always did.

<Callout title="Why it is this way">
This is what tmux-resurrect brings back after a reboot: it re-launches nvim in the pane's directory with the restore command, and without a written session that pane would come back as an empty editor in the right place. The keys sit on `<leader>s` and not persistence.nvim's documented `<leader>q` because `<leader>q` is already Quit here, and making it a prefix would leave every quit waiting `timeoutlen` for a second key.
</Callout>

See also: [`<leader>w and <leader>q`](/docs/manual/edit#leader-w-and-leader-q)  
Spec: `prds/07-multiplexer/06-nvim-session/prd.md`
