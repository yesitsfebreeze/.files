---
title: "Drop a directory into a container and work inside it"
description: "One capsule per directory, your credentials, no re-authentication."
---

{/* GENERATED by scripts/generate-manual.mjs — edit the .nuon surfaces, not this file. */}

One capsule per directory, your credentials, no re-authentication. Every entry links to where the guide explains it.

| | what it does | where | guide |
|---|---|---|---|
| `Ctrl+Shift+D` | Drop this directory into a capsule | terminal | [Work inside a container](/docs/guide/containers#enter-capsule) |
| `Ctrl+Shift+B` | Rebuild the capsule image and start over | terminal | [Work inside a container](/docs/guide/containers#rebuild-capsule) |
| `Ctrl+Shift+S` | Reopen a recent workspace here | terminal | [Work inside a container](/docs/guide/containers#recent-workspace) |
| `Ctrl+Shift+O` | Reopen a recent workspace in a new window | terminal | [Work inside a container](/docs/guide/containers#recent-workspace) |
| `capsule [dir]` | Drop a directory into a dev container | shell | [Work inside a container](/docs/guide/containers#enter-capsule) |
| `capsule --rebuild` | Rebuild the image and recreate the container | shell | [Work inside a container](/docs/guide/containers#rebuild-capsule) |
| `capsule list` | See which capsules exist | shell | [Work inside a container](/docs/guide/containers#capsule-list) |
| `capsule clean` | Remove capsules you are finished with | shell | [Work inside a container](/docs/guide/containers#capsule-clean) |
| `credentials in a capsule` | Understand how a container gets your credentials | container | [Work inside a container](/docs/guide/containers#credentials-in-a-capsule) |
| `ssh in a capsule` | Understand how SSH works inside a capsule | container | [Work inside a container](/docs/guide/containers#ssh-in-a-capsule) |
