diff --git a/home/.chezmoiremove b/home/.chezmoiremove
index 64cbcab..bafd03a 100644
--- a/home/.chezmoiremove
+++ b/home/.chezmoiremove
@@ -11,3 +11,4 @@
 .config/television/cable/git-branch.toml
 .config/television/cable/git-files.toml
 .config/television/cable/zoxide.toml
+.config/nushell/litellm.nu
diff --git a/home/dot_config/nushell/config.nu b/home/dot_config/nushell/config.nu
index 6ed1630..2fc5101 100644
--- a/home/dot_config/nushell/config.nu
+++ b/home/dot_config/nushell/config.nu
@@ -233,7 +233,6 @@ source ~/.cache/nushell/init/television.nu
 # (it calls finder's defs).
 source ~/.config/nushell/pass.nu
 source ~/.config/nushell/claude.nu
-source ~/.config/nushell/litellm.nu
 source ~/.config/nushell/recents.nu
 source ~/.config/nushell/zoxide.nu
 source ~/.config/nushell/history.nu
diff --git a/home/dot_config/nushell/help/manual/guide/agents.md b/home/dot_config/nushell/help/manual/guide/agents.md
index bd01bc9..8db8604 100644
--- a/home/dot_config/nushell/help/manual/guide/agents.md
+++ b/home/dot_config/nushell/help/manual/guide/agents.md
@@ -30,62 +30,6 @@ See also: [`cr [...args]`](./agents.md#cr-args) · [`credentials in a capsule`](
 
 See also: [`cc [...args]`](./agents.md#cc-args)
 
-## `cll [model]`
-
-**Start Claude Code on a chosen model**
-
-*shell*
-
-Run `cll` with no argument and a picker opens, grouped by provider; pick a row and Claude Code starts on that model. `cll <model>` skips the picker. Rows named `native:*` bypass the proxy and run on the Max plan; everything else goes through the local proxy, which `cll` starts itself when it is down — the first launch of the day waits a few seconds for it, later ones find it live. The picked model's context window is declared at launch, so auto-compact fires against what the model actually serves — 1M stays 1M instead of compacting at 200K.
-
-> **Why it is this way**
->
-> The picker lives in the script rather than in the shell, so every shell gets the same one. An earlier split — a nushell picker calling a script launcher — meant a stale shell called the script with no model at all and fell into a fallback path nothing had ever exercised.
-
-See also: [`llm`](./agents.md#llm) · [`llm quota`](./agents.md#llm-quota)
-
-## `llm`
-
-**See every model and what is left on it**
-
-*shell*
-
-Run `llm` for the catalogue as a table: every model, who serves it, and the balance remaining. It is a nushell table, so `llm | where provider == openai` works.
-
-> **Why it is this way**
->
-> One table rather than a page per provider: choosing a model is a comparison, and a comparison wants rows.
-
-See also: [`cll [model]`](./agents.md#cll-model) · [`llm quota`](./agents.md#llm-quota) · [`llm regen`](./agents.md#llm-regen)
-
-## `llm quota`
-
-**Read or record the balance per provider**
-
-*shell*
-
-`llm quota` prints the balances. `llm quota refresh` re-fetches the ones that have an API; `llm quota set <provider> <amount>` records the ones only a dashboard can tell you.
-
-> **Why it is this way**
->
-> Two kinds of provider, one command: some publish a balance endpoint and some publish nothing at all, and the second kind is why `set` exists rather than an empty column.
-
-See also: [`llm`](./agents.md#llm) · [`cll [model]`](./agents.md#cll-model)
-
-## `llm regen`
-
-**Rebuild the proxy's routing from live inventories**
-
-*shell*
-
-Run `llm regen` after a provider gains or loses models: it rewrites the proxy's routing config from what the providers actually serve now, and tells you to restart the proxy to pick it up.
-
-> **Why it is this way**
->
-> The routing config is derived, never hand-edited — a hand-edited route outlives the model it names and fails at the moment you most want the model.
-
-See also: [`llm`](./agents.md#llm) · [`cll [model]`](./agents.md#cll-model)
-
 ## Search with rg, find with fd, pick with tv
 
 *shell*
@@ -146,7 +90,7 @@ Press `<leader>xc` to spawn Claude in a split to the right of the editor — cll
 >
 > Inside tmux the terminal provider is claude-tmux, so this is a REAL pane of the `main` tmux session that tmux owns — addressable by its pane keys, F5-visible, and not nvim's built-in terminal. `<C-j>` is bound pane-locally, so nvim's own `<C-j>` window-down and every other pane are untouched. Outside a tmux session the provider falls back to snacks' terminal; headless runs do the same. The pane spawns through `cll` (claudecode's terminal_cmd), so model and proxy routing belong to cll: a `native:*` pick hands to `cc`'s login picker, a proxied pick sets CLAUDE_CONFIG_DIR to the litellm profile and exports the proxy — overriding whatever login resolution exported. Claudecode's own IDE env (the SSE port) rides the same spawn line, so diffs and @-mentions keep working under either route. The profile resolution in this file seeds only what cll does not govern: an inherited, logged-in CLAUDE_CONFIG_DIR for the native hop, resolved as above (`oauthAccount` decides, `~/.claude/.last-login` breaks the tie).
 
-See also: [`<leader>xf`](./agents.md#leader-xf) · [`cll [model]`](./agents.md#cll-model) · [`cc [...args]`](./agents.md#cc-args)
+See also: [`<leader>xf`](./agents.md#leader-xf) · [`cc [...args]`](./agents.md#cc-args)
 
 ## `<leader>xf`
 
@@ -186,7 +130,7 @@ See also: [`<leader>xr`](./agents.md#leader-xr)
 
 Press `<leader>xm` to pick the model the Claude pane runs on, without leaving the editor. That model choice lives in the pane's own profile, so it survives while the pane is open; the next fresh pane re-picks at spawn through cll. Native models are Claude Code's own, served by the Max plan.
 
-See also: [`<leader>xc`](./agents.md#leader-xc) · [`cll [model]`](./agents.md#cll-model)
+See also: [`<leader>xc`](./agents.md#leader-xc)
 
 ## `<leader>xb`
 
diff --git a/home/dot_config/nushell/help/manual/internals/neovim.md b/home/dot_config/nushell/help/manual/internals/neovim.md
index 4b920c2..148dad4 100644
--- a/home/dot_config/nushell/help/manual/internals/neovim.md
+++ b/home/dot_config/nushell/help/manual/internals/neovim.md
@@ -255,7 +255,7 @@ WHY snacks.nvim IS A DEPENDENCY EVEN THOUGH THE TMUX PROVIDER BYPASSES IT: claud
 
 THE SET MOVED FROM `<leader>a*` TO `<leader>x*` ON 2026-08-31, on the user's call: Claude gets a solo whichkey prefix that sits nowhere else, and `a` — a crowded, obvious letter — was the wrong place for it. The original sweep is still the method: this config binds -, |, a, b, bd, c, ca, cf, e, f, fb, ff, fg, fh, p, q, r, rn, t, tt, w (lua/config/keymaps.lua and the plugin `keys` stubs) and nothing on `x`, which is what makes `x` free. `x` is also why the map list shows `<leader>x` with desc `Claude`, the group's whole name. which-key needs no group entry: the bare `{ "<leader>x", desc = ... }` map is a prefix with live lazy stubs under it, and which-key renders it the same way it renders `f` (measured shape, see lua/plugins/which-key.lua).
 
-EPI Constraint, superseded 2026-08-31: claudecode launches the `claude` CLI directly, so it originally bypassed the `cc` profile picker and ran the default profile — which after the multi-login work meant an editor-side Claude that was not logged in. Superseded again the same day: the pane now spawns through **`cll`** (`terminal_cmd = "cll"` in claude.lua), so the model is picked at spawn and cll owns the routing — a `native:*` pick execs through `cc`'s login picker, a proxied pick sets `CLAUDE_CONFIG_DIR` to the litellm profile and exports the proxy pair, overriding whatever resolution exported. The Lua resolution below still earns its keep: it seeds the `CLAUDE_CONFIG_DIR` that cll's *native* branch inherits when it does not run its own picker, and it decides nothing else. The login-resolution rule stands: an inherited, logged-in `CLAUDE_CONFIG_DIR` is left alone; else the profile `~/.claude/.last-login` names **when it is actually logged in**, the test being `"oauthAccount"` in the profile's `.claude.json` (`settings.json` alone exists in logged-out profiles, seeded by `claude.nu`'s `_claude_share` on first pick, so file existence decides nothing); else nothing is exported and Claude's own login flow runs. The env rides claudecode's `env` option; the tmux and snacks providers both receive it (tmux prefixes the command, termopen layers it). Resolution happens when the plugin first loads, not per spawn — a fresh nvim after running `cc` picks up the new login, a long-running instance does not. Claudecode's IDE env (`CLAUDE_CODE_SSE_PORT`, `ENABLE_IDE_INTEGRATION`) is injected independently of `terminal_cmd`, so the WebSocket integration survives the detour through cll — measured on the cll side: neither branch unsets those two, and the SSE port survives `exec`.
+EPI Constraint, superseded 2026-08-31: claudecode launches the `claude` CLI directly, so it originally bypassed the `cc` profile picker and ran the default profile — which after the multi-login work meant an editor-side Claude that was not logged in. Superseded again the same day: the pane now spawns through **`cll`** (`terminal_cmd = "cll"` in claude.lua), so the model is picked at spawn and cll owns the routing. `cll` is not this repo's — it ships from its own project and puts itself on PATH; with that project uninstalled the pane does not open until `terminal_cmd` is set to `claude` — a `native:*` pick execs through `cc`'s login picker, a proxied pick sets `CLAUDE_CONFIG_DIR` to the litellm profile and exports the proxy pair, overriding whatever resolution exported. The Lua resolution below still earns its keep: it seeds the `CLAUDE_CONFIG_DIR` that cll's *native* branch inherits when it does not run its own picker, and it decides nothing else. The login-resolution rule stands: an inherited, logged-in `CLAUDE_CONFIG_DIR` is left alone; else the profile `~/.claude/.last-login` names **when it is actually logged in**, the test being `"oauthAccount"` in the profile's `.claude.json` (`settings.json` alone exists in logged-out profiles, seeded by `claude.nu`'s `_claude_share` on first pick, so file existence decides nothing); else nothing is exported and Claude's own login flow runs. The env rides claudecode's `env` option; the tmux and snacks providers both receive it (tmux prefixes the command, termopen layers it). Resolution happens when the plugin first loads, not per spawn — a fresh nvim after running `cc` picks up the new login, a long-running instance does not. Claudecode's IDE env (`CLAUDE_CODE_SSE_PORT`, `ENABLE_IDE_INTEGRATION`) is injected independently of `terminal_cmd`, so the WebSocket integration survives the detour through cll — measured on the cll side: neither branch unsets those two, and the SSE port survives `exec`.
 
 ```lua
 cmd = {
diff --git a/home/dot_config/nushell/help/manual/internals/nushell-modules.md b/home/dot_config/nushell/help/manual/internals/nushell-modules.md
index 1d77ba9..9594da6 100644
--- a/home/dot_config/nushell/help/manual/internals/nushell-modules.md
+++ b/home/dot_config/nushell/help/manual/internals/nushell-modules.md
@@ -760,44 +760,6 @@ def --wrapped cr [...args: string] { _claude_run (["--resume"] ++ $args) }
 
 cr — like cc, but resume a session within the chosen profile.
 
-## `litellm.nu`
-
-```
-def _llm_rows [] {
-```
-
-litellm — one launcher across every model provider we can reach.
-
-`cll` picks a model and starts Claude Code on it; `llm` is the catalogue and `llm quota` the balances. The heavy lifting lives in ~/.local/bin/{cll,llm-quota} so every non-nu caller shares one implementation — this module is the interactive skin: the fuzzy picker and tab-completion.
-
-The picker is `input list --fuzzy`, the same one `_claude_login` uses for the cc profile list, so fzf stays where it belongs (zoxide's zi/cdi). Catalogue rows decorated with the balance of whichever provider serves first. A model is only as usable as the credit behind it, so the number rides along.
-
-```
-def --wrapped cll [model?: string@_cll_models, ...args] {
-```
-
-cll — launch Claude Code on a chosen model. No argument opens cll's own fzf picker, grouped by provider; `native:*` rows bypass the proxy and run on the Max plan, everything else goes through litellm with its fallback chain.
-
-The picker lives in the script, not here, so every shell gets the same one — an earlier split (nushell picker, script launcher) meant a stale shell called the script with no model and fell into a fallback path nothing had exercised.
-
-```
-def llm [] { _llm_rows }
-```
-
-llm — the catalogue as a table: every model, who serves it, what's left.
-
-```
-def "llm quota" [...args] { ^llm-quota ...$args }
-```
-
-llm quota — balances per provider. `refresh` re-fetches what has an API; `set` records what only a dashboard can tell us (zenmux, opencode).
-
-```
-def "llm regen" [] {
-```
-
-llm regen — rebuild the litellm routing config from live provider inventories, then restart the proxy so it picks the new routes up.
-
 ## `pass.nu`
 
 ```
diff --git a/home/dot_config/nushell/help/manual/reference/agents.md b/home/dot_config/nushell/help/manual/reference/agents.md
index ea1714f..fb75d13 100644
--- a/home/dot_config/nushell/help/manual/reference/agents.md
+++ b/home/dot_config/nushell/help/manual/reference/agents.md
@@ -12,10 +12,6 @@ Every entry links to where the guide explains it.
 | `cr [...args]` | Resume a Claude session in this directory | shell | [Run coding agents](../guide/agents.md#cr-args) |
 | `help --json` | Read this manual as structured data | shell | [Run coding agents](../guide/agents.md#help-json) |
 | `idioms` | Search with rg, find with fd, pick with tv | shell | [Run coding agents](../guide/agents.md#idioms) |
-| `cll [model]` | Start Claude Code on a chosen model | shell | [Run coding agents](../guide/agents.md#cll-model) |
-| `llm` | See every model and what is left on it | shell | [Run coding agents](../guide/agents.md#llm) |
-| `llm quota` | Read or record the balance per provider | shell | [Run coding agents](../guide/agents.md#llm-quota) |
-| `llm regen` | Rebuild the proxy's routing from live inventories | shell | [Run coding agents](../guide/agents.md#llm-regen) |
 | `pearde [cmd]` | Work the PRD board this repository plans with | shell | [Run coding agents](../guide/agents.md#pearde-cmd) |
 | `<leader>x` | Show what <leader>x can do | nvim normal | [Run coding agents](../guide/agents.md#leader-x) |
 | `<leader>xc` | Toggle Claude | nvim normal | [Run coding agents](../guide/agents.md#leader-xc) |
diff --git a/home/dot_config/nushell/help/nvim.nuon b/home/dot_config/nushell/help/nvim.nuon
index d33d683..32b25e0 100644
--- a/home/dot_config/nushell/help/nvim.nuon
+++ b/home/dot_config/nushell/help/nvim.nuon
@@ -318,7 +318,7 @@
         task: "agents"
         step: 12
         mode: "nvim:normal"
-        also: ["<leader>xf", "cll [model]", "cc [...args]"]
+        also: ["<leader>xf", "cc [...args]"]
         why: "Inside tmux the terminal provider is claude-tmux, so this is a REAL pane of the `main` tmux session that tmux owns — addressable by its pane keys, F5-visible, and not nvim's built-in terminal. `<C-j>` is bound pane-locally, so nvim's own `<C-j>` window-down and every other pane are untouched. Outside a tmux session the provider falls back to snacks' terminal; headless runs do the same. The pane spawns through `cll` (claudecode's terminal_cmd), so model and proxy routing belong to cll: a `native:*` pick hands to `cc`'s login picker, a proxied pick sets CLAUDE_CONFIG_DIR to the litellm profile and exports the proxy — overriding whatever login resolution exported. Claudecode's own IDE env (the SSE port) rides the same spawn line, so diffs and @-mentions keep working under either route. The profile resolution in this file seeds only what cll does not govern: an inherited, logged-in CLAUDE_CONFIG_DIR for the native hop, resolved as above (`oauthAccount` decides, `~/.claude/.last-login` breaks the tie)."
     }
     {
@@ -359,7 +359,7 @@
         task: "agents"
         step: 17
         mode: "nvim:normal"
-        also: ["<leader>xc", "cll [model]"]
+        also: ["<leader>xc"]
     }
     {
         key: "<leader>xb"
diff --git a/home/dot_config/nushell/help/shell.nuon b/home/dot_config/nushell/help/shell.nuon
index e9dc30a..ac117dc 100644
--- a/home/dot_config/nushell/help/shell.nuon
+++ b/home/dot_config/nushell/help/shell.nuon
@@ -423,50 +423,6 @@
         mode: "shell"
         also: ["theme"]
     }
-    {
-        cmd: "cll [model]"
-        title: "Start Claude Code on a chosen model"
-        use: "Run `cll` with no argument and a picker opens, grouped by provider; pick a row and Claude Code starts on that model. `cll <model>` skips the picker. Rows named `native:*` bypass the proxy and run on the Max plan; everything else goes through the local proxy, which `cll` starts itself when it is down — the first launch of the day waits a few seconds for it, later ones find it live. The picked model's context window is declared at launch, so auto-compact fires against what the model actually serves — 1M stays 1M instead of compacting at 200K."
-        topic: "agents"
-        task: "agents"
-        step: 3
-        mode: "shell"
-        also: ["llm", "llm quota"]
-        why: "The picker lives in the script rather than in the shell, so every shell gets the same one. An earlier split — a nushell picker calling a script launcher — meant a stale shell called the script with no model at all and fell into a fallback path nothing had ever exercised."
-    }
-    {
-        cmd: "llm"
-        title: "See every model and what is left on it"
-        use: "Run `llm` for the catalogue as a table: every model, who serves it, and the balance remaining. It is a nushell table, so `llm | where provider == openai` works."
-        topic: "agents"
-        task: "agents"
-        step: 4
-        mode: "shell"
-        also: ["cll [model]", "llm quota", "llm regen"]
-        why: "One table rather than a page per provider: choosing a model is a comparison, and a comparison wants rows."
-    }
-    {
-        cmd: "llm quota"
-        title: "Read or record the balance per provider"
-        use: "`llm quota` prints the balances. `llm quota refresh` re-fetches the ones that have an API; `llm quota set <provider> <amount>` records the ones only a dashboard can tell you."
-        topic: "agents"
-        task: "agents"
-        step: 5
-        mode: "shell"
-        also: ["llm", "cll [model]"]
-        why: "Two kinds of provider, one command: some publish a balance endpoint and some publish nothing at all, and the second kind is why `set` exists rather than an empty column."
-    }
-    {
-        cmd: "llm regen"
-        title: "Rebuild the proxy's routing from live inventories"
-        use: "Run `llm regen` after a provider gains or loses models: it rewrites the proxy's routing config from what the providers actually serve now, and tells you to restart the proxy to pick it up."
-        topic: "agents"
-        task: "agents"
-        step: 6
-        mode: "shell"
-        also: ["llm", "cll [model]"]
-        why: "The routing config is derived, never hand-edited — a hand-edited route outlives the model it names and fails at the moment you most want the model."
-    }
     {
         cmd: "pearde [cmd]"
         title: "Work the PRD board this repository plans with"
diff --git a/home/dot_config/nushell/litellm.nu b/home/dot_config/nushell/litellm.nu
deleted file mode 100644
index 9601346..0000000
--- a/home/dot_config/nushell/litellm.nu
+++ /dev/null
@@ -1,39 +0,0 @@
-# litellm.nu
-# Why this file is shaped the way it is:
-#   manual → internals/nushell-modules
-
-def _llm_rows [] {
-  let cat = (do { ^cll --catalog } | complete)
-  if $cat.exit_code != 0 { return [] }
-  let q = (do { ^llm-quota --json } | complete)
-  let bal = (if $q.exit_code == 0 { $q.stdout | from json } else { {} })
-  let known = ($bal | columns)
-
-  $cat.stdout | from json | each {|r|
-    let head = ($r.providers | first)
-    let left = (if ($head in $known) {
-      let e = ($bal | get $head)
-      if $e.unit? == "free" { "free" } else if ($e.remaining? | is-empty) { "?" } else { $e.remaining | into string }
-    } else if $head == "max-plan" { "plan" } else { "?" })
-    {
-      model: $r.name
-      via: ($r.providers | str join " → ")
-      left: $left
-    }
-  }
-}
-
-def _cll_models [] { _llm_rows | get model }
-
-def --wrapped cll [model?: string@_cll_models, ...args] {
-  if ($model | is-not-empty) { ^cll $model ...$args } else { ^cll ...$args }
-}
-
-def llm [] { _llm_rows }
-
-def "llm quota" [...args] { ^llm-quota ...$args }
-
-def "llm regen" [] {
-  ^litellm-gen-config
-  print "regenerated ~/.config/litellm/config.yaml — restart litellm-up to apply"
-}
diff --git a/home/dot_config/nvim/lua/plugins/claude.lua b/home/dot_config/nvim/lua/plugins/claude.lua
index 820ad76..a6c56a0 100644
--- a/home/dot_config/nvim/lua/plugins/claude.lua
+++ b/home/dot_config/nvim/lua/plugins/claude.lua
@@ -62,9 +62,12 @@ return {
         })
       end
 
-      -- Login profile is `cll`'s job (~/.local/bin/cll:212-221 resolves the
-      -- same CLAUDE_CONFIG_DIR from .last-login/.claude.json) — terminal_cmd
-      -- already spawns through it, so nothing here needs to duplicate it.
+      -- Login profile is `cll`'s job — it resolves the same
+      -- CLAUDE_CONFIG_DIR from .last-login/.claude.json, and terminal_cmd
+      -- already spawns through it, so nothing here duplicates it. `cll` is
+      -- not this repo's: it ships from its own project and puts itself on
+      -- PATH. TRAP: with that project uninstalled the pane will not open —
+      -- set terminal_cmd = "claude" to go direct.
       require("claudecode").setup(opts)
     end,
   },
diff --git a/home/dot_local/bin/executable_cll b/home/dot_local/bin/executable_cll
deleted file mode 100755
index 17d93dd..0000000
--- a/home/dot_local/bin/executable_cll
+++ /dev/null
@@ -1,294 +0,0 @@
-#!/usr/bin/env bash
-# cll — Claude Code against the local LiteLLM proxy, or natively on the Max plan.
-#
-#   cll                    pick a model (fzf, grouped by provider)
-#   cll opus-5             a proxied model, using its normal fallback chain
-#   cll opus-5@openrouter  pin one provider hop
-#   cll native:opus        Max plan — launched through cc's login picker
-#   cll --list             model names, one per line
-#   cll --catalog          JSON: name, providers, kind
-#
-# Switch mid-session with `/model <name>`; cll regenerates the picker list from
-# the proxy on every launch. That choice persists into CLAUDE_CONFIG_DIR, which
-# is why cll runs its own `litellm` profile — it must never rewrite the `model`
-# field of the Max-plan ~/.claude/settings.json.
-#
-# Launches with --dangerously-skip-permissions, like `cc`/`cr`.
-set -euo pipefail
-
-. "$(dirname "$(command -v litellm-env)")/litellm-env"
-
-CATALOG="$HOME/.local/state/litellm/catalog.json"
-CONTEXTS="$HOME/.local/state/litellm/contexts.json"
-# Claude Code's own aliases, served by the Max plan rather than any provider.
-NATIVE=(opus sonnet haiku fable)
-
-# Resolved context window for a catalog alias (written by litellm-gen-config).
-# Prints nothing when the file, the alias or the number itself is absent — the
-# caller then leaves Claude Code on its own default rather than exporting 0.
-ctx_lookup() {
-  python3 - "$1" "$CONTEXTS" <<'PY'
-import json, sys
-try:
-    w = json.load(open(sys.argv[2])).get(sys.argv[1], {}).get("context_window")
-    if w:
-        print(w)
-except Exception:
-    pass
-PY
-}
-
-catalog_json() {
-  CLL_NATIVE="${NATIVE[*]}" python3 - "$CATALOG" <<'PY'
-import json, os, sys
-rows = []
-for a in os.environ["CLL_NATIVE"].split():
-    rows.append({"name": f"native:{a}", "providers": ["max-plan"], "kind": "native"})
-try:
-    cat = json.load(open(sys.argv[1]))
-except Exception:
-    cat = {}
-for name, chain in cat.items():
-    rows.append({"name": name, "providers": chain, "kind": "proxy"})
-    for p in chain[1:]:
-        rows.append({"name": f"{name}@{p}", "providers": [p], "kind": "pinned"})
-print(json.dumps(rows))
-PY
-}
-
-# Names only. The reader (a completer, `head`) may close the pipe early, and an
-# unguarded print then dies with BrokenPipeError noise on stderr — so the JSON
-# is decoded in one process that swallows EPIPE rather than piped between two.
-names_only() {
-  catalog_json | python3 -c '
-import sys, json
-try:
-    for r in json.load(sys.stdin):
-        print(r["name"])
-    sys.stdout.flush()
-except BrokenPipeError:
-    pass
-except Exception:
-    sys.exit(1)
-' 2>/dev/null
-}
-
-# Picker rows, grouped by the provider that serves each model first, with the
-# remaining balance alongside — a model is only as usable as the credit behind
-# it. Emitted as TAB-separated fields for fzf; see the --nth flags at the call.
-picker_rows() {
-  CLL_ROWS="$(catalog_json)" CLL_QUOTA="$(llm-quota --json 2>/dev/null || echo '{}')" python3 <<'PY'
-import json, os
-rows = json.loads(os.environ["CLL_ROWS"])
-quota = json.loads(os.environ["CLL_QUOTA"] or "{}")
-
-def left(prov):
-    if prov == "max-plan":
-        return "plan"
-    e = quota.get(prov)
-    if not e:
-        return "?"
-    if e.get("unit") == "free":
-        return "free"
-    r = e.get("remaining")
-    return "?" if r is None else f"{r:,.2f}"
-
-groups, order = {}, []
-for r in rows:
-    p = r["providers"][0]
-    if p not in order:
-        order.append(p); groups[p] = []
-    groups[p].append(r)
-
-DIM, CYAN, RESET = "\033[2m", "\033[36m", "\033[0m"
-width = max((len(r["name"]) for r in rows), default=20) + 2
-pwidth = max((len(p) for p in order), default=8) + 1
-
-# Every row carries its OWN provider column, not just the group header above
-# it. fzf drops non-matching lines, so a header cannot stay pinned over its
-# group once you type — filtering to "opus" would otherwise leave a column of
-# models with no way to tell which router each came from. The header gives the
-# shape at rest; the column is what survives a query.
-for p in order:
-    bal = left(p)
-    rule = "─" * max(4, width + pwidth - len(p) - len(bal))
-    print(f"__hdr__\t{DIM}── {p} {rule} {bal}{RESET}\t")
-    for r in groups[p]:
-        chain = " → ".join(r["providers"][1:])
-        tail = f"{DIM}↳ {chain}{RESET}" if chain else ""
-        print(f"{r['name']}\t{CYAN}{p:<{pwidth}}{RESET} {r['name']:<{width}}\t{tail}")
-PY
-}
-
-case "${1:-}" in
-  --catalog) catalog_json; exit 0 ;;
-  --list)    names_only; exit 0 ;;
-  --rows)    picker_rows; exit 0 ;;
-esac
-
-MODEL=""
-if [ $# -gt 0 ] && [ "$1" != "--" ] && [[ "$1" != -* ]]; then
-  MODEL="$1"; shift
-fi
-[ "${1:-}" = "--" ] && shift
-
-if [ -z "$MODEL" ]; then
-  if command -v fzf >/dev/null 2>&1; then
-    # Rows are TAB-separated: key, padded model name, fallback chain.
-    #
-    # --nth=1 NOT =2, and the reason is not obvious: --with-nth re-indexes the
-    # fields, so --nth counts against the TRANSFORMED line, where the name is
-    # field 1 and the chain field 2 — the key is already gone. Measured on fzf
-    # 0.74.3: with --nth=2, `opus` matched nothing at all, because it was
-    # searching the chain column. Searching field 1 alone is also what makes
-    # group headers behave: their name column is a rule, so `opus` drops them
-    # and `zenmux` keeps that one header — filter by model, or by group.
-    #
-    # --accept-nth=1 hands back the bare key. fzf has no unselectable row, so a
-    # header's key is a sentinel and Enter on one simply re-opens the picker.
-    while :; do
-      MODEL="$(picker_rows | fzf --ansi --delimiter=$'\t' \
-        --with-nth=2.. --nth=1 --accept-nth=1 \
-        --height=70% --reverse --prompt='model> ' \
-        --header='claude via litellm — grouped by provider; type to filter models')" || MODEL=""
-      [ "$MODEL" = "__hdr__" ] || break
-    done
-  else
-    # Plain `while read`, not `mapfile`: this machine's only bash is /bin/bash
-    # 3.2 (macOS ships no newer one and no homebrew bash is installed), where
-    # that builtin does not exist.
-    NAMES=""
-    while IFS= read -r line; do NAMES="$NAMES$line
-"; done <<EOF
-$(names_only)
-EOF
-    printf '%s' "$NAMES" | nl -w3 -s') '
-    printf 'model number: '; read -r n
-    MODEL="$(printf '%s' "$NAMES" | sed -n "${n}p")"
-  fi
-fi
-[ -n "$MODEL" ] || { echo "cll: no model selected" >&2; exit 1; }
-
-# --- native: Claude Code's own models on the Max plan, launched the way cc
-# launches them — no proxy in the way, and no proxy env inherited either.
-if [[ "$MODEL" == native:* ]]; then
-  M="${MODEL#native:}"
-  # "No proxy" must mean no proxy: unset, not merely don't-set. Launched from
-  # inside a cll session the parent exports ANTHROPIC_BASE_URL/AUTH_TOKEN, and
-  # an inherited pair would route this "native" hop through the proxy anyway.
-  unset ANTHROPIC_BASE_URL ANTHROPIC_AUTH_TOKEN ANTHROPIC_API_KEY \
-        ANTHROPIC_MODEL ANTHROPIC_SMALL_FAST_MODEL ANTHROPIC_DEFAULT_HAIKU_MODEL \
-        CLAUDE_CODE_MAX_CONTEXT_TOKENS
-
-  if [ -t 0 ] && command -v nu >/dev/null 2>&1; then
-    # Interactive: this is cc's job. Its login picker chooses the profile and
-    # _claude_run supplies the --dangerously-skip-permissions launch, so a
-    # native hop behaves exactly like `cc --model <m>` — one implementation
-    # of "launch Claude on a login", not a second one here. Args travel via
-    # the environment as JSON: splicing argv into a nu command string would
-    # be a quoting bug waiting for the first space.
-    echo "cll: $M — max plan, via cc (login picker)" >&2
-    export CLL_NATIVE_MODEL="$M"
-    if [ $# -gt 0 ]; then
-      export CLL_NATIVE_ARGS="$(printf '%s\0' "$@" | python3 -c '
-import json, sys
-print(json.dumps(sys.stdin.buffer.read().decode("utf-8", "surrogateescape").split("\0")[:-1]))
-')"
-    else
-      export CLL_NATIVE_ARGS="[]"
-    fi
-    exec nu -c 'source ~/.config/nushell/config.nu
-      cc ...([--model, $env.CLL_NATIVE_MODEL] ++ ($env.CLL_NATIVE_ARGS? | default "[]" | from json))'
-  fi
-
-  # No TTY (scripted) or no nu: cc's picker cannot run — `input list` without
-  # a terminal errors out. Resolve the login the way its picker would have
-  # offered: an inherited profile that is actually logged in, else the last
-  # one cc recorded, else the default ~/.claude.
-  d="${CLAUDE_CONFIG_DIR:-}"
-  if [ -z "$d" ] || ! grep -q '"oauthAccount"' "$d/.claude.json" 2>/dev/null; then
-    d=""
-    LAST="$(cat "$HOME/.claude/.last-login" 2>/dev/null || true)"
-    if [ -n "$LAST" ] && [ "$LAST" != "default" ] && \
-       grep -q '"oauthAccount"' "$HOME/.claude/$LAST/.claude.json" 2>/dev/null; then
-      d="$HOME/.claude/$LAST"
-    fi
-  fi
-  if [ -n "$d" ]; then export CLAUDE_CONFIG_DIR="$d"; else unset CLAUDE_CONFIG_DIR; fi
-  echo "cll: $M — max plan${d:+ ($d)}" >&2
-  exec claude --model "$M" --dangerously-skip-permissions "$@"
-fi
-
-if ! curl -sf --max-time 2 "$LITELLM_BASE/health/liveliness" >/dev/null 2>&1; then
-  echo "cll: LiteLLM proxy is not running on ${LITELLM_BASE#http://} — start it with: litellm-up" >&2
-  exit 1
-fi
-
-# --- proxied: its own login profile, so /model can't leak into the Max config -
-ROOT="$HOME/.claude"; PROFILE="$ROOT/litellm"
-mkdir -p "$PROFILE"
-for item in plugins projects sessions session-env history.jsonl cache file-history \
-            shell-snapshots agent-memory hooks hub jobs plans backups paste-cache \
-            context-mode debug tasks teams telemetry; do
-  [ -e "$ROOT/$item" ] && [ ! -e "$PROFILE/$item" ] && ln -s "$ROOT/$item" "$PROFILE/$item"
-done
-for f in .claude.json settings.json settings.local.json; do
-  [ -f "$ROOT/$f" ] && [ ! -f "$PROFILE/$f" ] && cp "$ROOT/$f" "$PROFILE/$f"
-done
-[ -f "$PROFILE/settings.json" ] || echo '{}' > "$PROFILE/settings.json"
-
-# Rebuild /model's list from the catalog so runtime switching offers the same
-# names as the launch picker. Only `modelPicker` is touched — the `model` key
-# /model writes, and every other setting, is preserved.
-CLL_ROWS="$(catalog_json)" python3 - "$PROFILE/settings.json" <<'PY'
-import json, os, sys
-rows = json.loads(os.environ["CLL_ROWS"])
-path = sys.argv[1]
-try:
-    s = json.load(open(path))
-except Exception:
-    s = {}
-opts = []
-for r in rows:
-    if r["kind"] == "native":
-        continue  # the Max-plan models aren't reachable through the proxy
-    opts.append({"model": r["name"], "label": r["name"],
-                 "description": " → ".join(r["providers"])})
-s["modelPicker"] = {"options": opts, "replaceBuiltInOptions": True}
-json.dump(s, open(path, "w"), indent=2)
-PY
-
-export CLAUDE_CONFIG_DIR="$PROFILE"
-export ANTHROPIC_BASE_URL="$LITELLM_BASE"
-export ANTHROPIC_AUTH_TOKEN="$LITELLM_MASTER_KEY"
-export ANTHROPIC_MODEL="$MODEL"
-export ANTHROPIC_SMALL_FAST_MODEL="${CLL_SMALL_MODEL:-claude-haiku-4-5-20251001}"
-export ANTHROPIC_DEFAULT_HAIKU_MODEL="$ANTHROPIC_SMALL_FAST_MODEL"
-unset ANTHROPIC_API_KEY
-
-# Context window. Claude Code sizes its own context against the model *id* it
-# was given, not against anything the backend reports: an id it does not
-# recognise — everything here that is not `claude-*` — silently gets the 200K
-# assumption, so auto-compact fires far too early for a 1M model and never for
-# one that truncates at 131K. There is no per-model mapping in Claude Code, so
-# the picked model's window is declared per launch with
-# CLAUDE_CODE_MAX_CONTEXT_TOKENS (contexts.json records where each number came
-# from). A `claude-*` alias is left alone on purpose: for a real Claude id that
-# variable takes effect only with DISABLE_COMPACT, which would turn compaction
-# off entirely — Claude Code's own knowledge of the id beats that trade. A
-# pinned hop looks its window up under the base alias: it is a provider fix,
-# not a different model.
-case "$MODEL" in
-  claude-*) unset CLAUDE_CODE_MAX_CONTEXT_TOKENS ;;
-  *)
-    W="$(ctx_lookup "${MODEL%%@*}")"
-    if [ -n "$W" ]; then
-      export CLAUDE_CODE_MAX_CONTEXT_TOKENS="$W"
-    else
-      unset CLAUDE_CODE_MAX_CONTEXT_TOKENS
-    fi
-    ;;
-esac
-
-echo "cll: $MODEL via litellm" >&2
-exec claude --dangerously-skip-permissions "$@"
diff --git a/home/dot_local/bin/executable_litellm-env b/home/dot_local/bin/executable_litellm-env
deleted file mode 100755
index 33555fc..0000000
--- a/home/dot_local/bin/executable_litellm-env
+++ /dev/null
@@ -1,54 +0,0 @@
-#!/usr/bin/env bash
-# litellm-env — resolve LiteLLM secrets without storing a second copy of them.
-#
-# Provider keys come from pi's credential store (~/.pi/agent/auth.json), which is
-# already the machine-local source of truth. The proxy's own master key is
-# generated on first use into ~/.local/state/litellm/master.key.
-#
-# Nothing here is committable, and nothing here duplicates a secret.
-# Usage: `. litellm-env` (sourced), or `litellm-env` to print the master key.
-set -euo pipefail
-
-LITELLM_PORT="${LITELLM_PORT:-4000}"
-LITELLM_BASE="http://127.0.0.1:${LITELLM_PORT}"
-_auth="$HOME/.pi/agent/auth.json"
-_state="$HOME/.local/state/litellm"
-_master_file="$_state/master.key"
-
-if [ ! -f "$_master_file" ]; then
-  mkdir -p "$_state"
-  ( umask 077; python3 -c 'import secrets; print("sk-litellm-"+secrets.token_urlsafe(24))' > "$_master_file" )
-fi
-LITELLM_MASTER_KEY="$(cat "$_master_file")"
-
-if [ -f "$_auth" ]; then
-  # `access` for OAuth-style entries, `key` for plain API keys.
-  eval "$(python3 - "$_auth" <<'PY'
-import json, sys, shlex
-try:
-    a = json.load(open(sys.argv[1]))
-except Exception:
-    a = {}
-def get(name):
-    e = a.get(name) or {}
-    return e.get("key") or e.get("access") or ""
-for var, prov in (("OPENROUTER_API_KEY", "openrouter"), ("ZENMUX_API_KEY", "zenmux"),
-                  ("OPENCODE_API_KEY", "opencode"), ("OPENCODE_GO_API_KEY", "opencode-go")):
-    v = get(prov)
-    if v:
-        print(f"export {var}={shlex.quote(v)}")
-PY
-)"
-fi
-
-# Ollama needs a non-empty key to satisfy the OpenAI client; it ignores the value.
-export OPENROUTER_API_KEY="${OPENROUTER_API_KEY:-}" ZENMUX_API_KEY="${ZENMUX_API_KEY:-}"
-export OPENCODE_API_KEY="${OPENCODE_API_KEY:-}" OPENCODE_GO_API_KEY="${OPENCODE_GO_API_KEY:-}"
-export LITELLM_MASTER_KEY LITELLM_PORT LITELLM_BASE
-
-# Executed rather than sourced: print the master key (for scripting).
-# `${BASH_SOURCE[0]:-}` — sourcing from a non-bash shell leaves it unset, and
-# `set -u` would abort the caller's shell rather than fall through.
-if [ "${BASH_SOURCE[0]:-}" = "${0}" ]; then
-  echo "$LITELLM_MASTER_KEY"
-fi
diff --git a/home/dot_local/bin/executable_litellm-gen-config b/home/dot_local/bin/executable_litellm-gen-config
deleted file mode 100755
index 11f7b4c..0000000
--- a/home/dot_local/bin/executable_litellm-gen-config
+++ /dev/null
@@ -1,293 +0,0 @@
-#!/usr/bin/env python3
-"""Generate ~/.config/litellm/config.yaml from live provider inventories.
-
-Provider order per alias is the fallback chain: cheapest/most-funded first.
-Only deployments the provider actually serves are emitted.
-"""
-import json, os, urllib.request, sys
-
-HOME = os.path.expanduser("~")
-auth = json.load(open(f"{HOME}/.pi/agent/auth.json"))
-key = lambda p: (auth.get(p) or {}).get("key") or (auth.get(p) or {}).get("access") or ""
-
-PROVIDERS = {
-    # name: (base_url, env var, litellm prefix style)
-    "ollama":      ("http://127.0.0.1:11434/v1", None),
-    "zenmux":      ("https://zenmux.ai/api/v1", "ZENMUX_API_KEY"),
-    "opencode":    ("https://opencode.ai/zen/v1", "OPENCODE_API_KEY"),
-    "opencode-go": ("https://opencode.ai/zen/go/v1", "OPENCODE_GO_API_KEY"),
-    "openrouter":  ("https://openrouter.ai/api/v1", "OPENROUTER_API_KEY"),
-}
-# Preference order — zenmux is funded, openrouter is spent, ollama is free but weak.
-ORDER = ["zenmux", "opencode", "opencode-go", "openrouter"]
-
-def fetch(url, k):
-    """Model ids of a provider, with the context window each publishes
-    ({context_length, max_completion_tokens} — None where absent)."""
-    try:
-        r = urllib.request.Request(url + "/models", headers={"Authorization": "Bearer " + k})
-        got = {}
-        for m in json.load(urllib.request.urlopen(r, timeout=25))["data"]:
-            mx = m.get("max_completion_tokens")
-            if mx is None:
-                mx = (m.get("top_provider") or {}).get("max_completion_tokens")
-            got[m["id"]] = {"context": m.get("context_length"), "max": mx}
-        return got
-    except Exception as e:
-        print(f"  ! {url}: {e}", file=sys.stderr)
-        return {}
-
-def ollama_ctx(name):
-    """Trained window from the GGUF metadata (/api/show → `llama.context_length`
-    and friends). The USEABLE window is num_ctx: the daemon serves at its own
-    default (or OLLAMA_CONTEXT_LENGTH) unless the Modelfile sets num_ctx, and it
-    silently truncates rather than erroring when a request overstays — so a
-    trained max with no num_ctx is an upper bound the server may not honour.
-    Callers record the distinction instead of passing a bound off as a promise."""
-    try:
-        req = urllib.request.Request(
-            "http://127.0.0.1:11434/api/show",
-            data=json.dumps({"model": name}).encode(),
-            headers={"Content-Type": "application/json"})
-        d = json.load(urllib.request.urlopen(req, timeout=10))
-        lens = [v for k, v in d.get("model_info", {}).items()
-                if k.endswith(".context_length")]
-        params = d.get("model_info", {}).get("general.parameter_count")
-        p = d.get("parameters") or ""
-        nums = [int(t.split()[1]) for t in p.splitlines() if t.startswith("num_ctx ")]
-        if nums:
-            return {"context": min(nums), "max": None, "kind": "num_ctx",
-                    "params": params}
-        return {"context": min(lens) if lens else None, "max": None,
-                "kind": "trained", "params": params}
-    except Exception:
-        return {"context": None, "max": None}
-
-avail = {}
-# Context windows per provider, {provider: {model id: {context, max}}}. Every
-# client downstream of this generator used to carry a flat 200000/64000 for
-# every route (hardcoded in the pi models.json write below), which silently
-# under-windowed the 1M models and over-promised the 131K ones.
-ctx = {}
-for p in ("zenmux", "openrouter"):
-    got = fetch(PROVIDERS[p][0], key(p))
-    avail[p] = set(got)
-    ctx[p] = got
-store = json.load(open(f"{HOME}/.pi/agent/models-store.json"))
-for p in ("opencode", "opencode-go"):
-    models = store.get(p, {}).get("models", [])
-    avail[p] = {m["id"] for m in models}
-    # The store already rates context per model — read it, don't re-fetch.
-    ctx[p] = {m["id"]: {"context": m.get("contextWindow"), "max": m.get("maxTokens")}
-              for m in models}
-try:
-    tags = json.load(urllib.request.urlopen("http://127.0.0.1:11434/api/tags", timeout=5))
-    avail["ollama"] = {m["name"] for m in tags["models"]}
-    ctx["ollama"] = {m["name"]: ollama_ctx(m["name"])
-                     for m in tags["models"] if "embed" not in m["name"]}
-except Exception:
-    avail["ollama"] = set()
-
-for p in ORDER + ["ollama"]:
-    print(f"  {p}: {len(avail[p])} models", file=sys.stderr)
-
-# alias -> {provider: model id as that provider names it}
-def A(vendor, name, oc=None, go=None, ol=None):
-    d = {"zenmux": f"{vendor}/{name}", "openrouter": f"{vendor}/{name}"}
-    if oc: d["opencode"] = oc
-    if go: d["opencode-go"] = go
-    if ol: d["ollama"] = ol
-    return d
-
-ALIASES = {
-    # Anthropic
-    "opus-5":        A("anthropic", "claude-opus-5",   oc="claude-opus-5"),
-    "opus-4.8":      A("anthropic", "claude-opus-4.8", oc="claude-opus-4-8"),
-    "sonnet-5":      A("anthropic", "claude-sonnet-5", oc="claude-sonnet-5"),
-    "haiku-4.5":     A("anthropic", "claude-haiku-4.5", oc="claude-haiku-4-5"),
-    "fable-5":       A("anthropic", "claude-fable-5",  oc="claude-fable-5"),
-    # OpenAI
-    "gpt-5.6-sol":   A("openai", "gpt-5.6-sol",   oc="gpt-5.6-sol"),
-    "gpt-5.6-luna":  A("openai", "gpt-5.6-luna",  oc="gpt-5.6-luna", go="gpt-5.6-luna"),
-    "gpt-5.5":       A("openai", "gpt-5.5",       oc="gpt-5.5"),
-    "gpt-5.5-pro":   A("openai", "gpt-5.5-pro",   oc="gpt-5.5-pro"),
-    "gpt-5.3-codex": A("openai", "gpt-5.3-codex", oc="gpt-5.3-codex"),
-    "gpt-5.4-mini":  A("openai", "gpt-5.4-mini",  oc="gpt-5.4-mini"),
-    # Google
-    "gemini-3.1-pro":   {"zenmux": "google/gemini-3.1-pro-preview",
-                         "openrouter": "google/gemini-3.1-pro-preview",
-                         "opencode": "gemini-3.1-pro"},
-    "gemini-3.7-flash": A("google", "gemini-3.7-flash", oc="gemini-3.7-flash"),
-    # Open weights
-    "kimi-k3":         A("moonshotai", "kimi-k3", go="kimi-k3"),
-    "kimi-k2.7-code":  A("moonshotai", "kimi-k2.7-code", go="kimi-k2.7-code"),
-    "glm-5.3":         A("z-ai", "glm-5.3", go="glm-5.3"),
-    "glm-5.2":         A("z-ai", "glm-5.2", oc="glm-5.2", go="glm-5.2"),
-    "deepseek-v4-pro": A("deepseek", "deepseek-v4-pro", oc="deepseek-v4-pro", go="deepseek-v4-pro"),
-    "qwen3-max":       A("qwen", "qwen3-max"),
-}
-
-# Ollama is enumerated, not hand-listed: it serves BOTH local weights and
-# ollama.com cloud models (a `:cloud` tag — deepseek-v4-flash:cloud is 304B),
-# and the set changes whenever something is pulled. Embedding models are not
-# chat models, so they are excluded rather than offered as broken routes.
-for _name in sorted(avail.get("ollama", set())):
-    if "embed" in _name:
-        continue
-    _alias = _name.replace(":", "-")
-    if _alias not in ALIASES:
-        ALIASES[_alias] = {"ollama": _name}
-# Canonical Anthropic ids so Claude Code reads the right context window.
-CANONICAL = {
-    "claude-opus-5": "opus-5", "claude-sonnet-5": "sonnet-5",
-    "claude-haiku-4-5-20251001": "haiku-4.5", "claude-fable-5": "fable-5",
-}
-
-def deployment(prov, model_id, indent="      "):
-    base, envvar = PROVIDERS[prov]
-    if prov == "openrouter":
-        m, extra = f"openrouter/{model_id}", ""
-    else:
-        m, extra = f"openai/{model_id}", f", api_base: {base}"
-    k = f", api_key: os.environ/{envvar}" if envvar else ", api_key: ollama"
-    # Claude Code always sends `thinking`. A small local ollama model rejects it
-    # outright ("does not support thinking", measured on granite4:3b) and the
-    # global drop_params does not cover it — the param is valid upstream, it is
-    # the DEPLOYMENT that can't take it. Cloud models are the other way round:
-    # deepseek-v4-flash:cloud answered a reasoning_effort request with a
-    # populated `reasoning` field (measured 2026-08-25), so dropping there would
-    # throw away the capability. Hence local-only.
-    local_ollama = prov == "ollama" and not model_id.endswith(":cloud")
-    drop = ', additional_drop_params: ["thinking", "reasoning_effort"]' if local_ollama else ""
-    return f"{{ model: {m}{extra}{k}{drop} }}"
-
-lines = ["# GENERATED by scripts/gen-litellm-config.py — regenerate, don't hand-edit.",
-         "# Chain per alias is the fallback order; `<alias>@<provider>` pins one hop.",
-         "", "model_list:"]
-fallbacks, catalog = [], {}
-
-for alias, m in ALIASES.items():
-    chain = [p for p in ORDER + ["ollama"] if p in m and m[p] in avail.get(p, set())]
-    if not chain:
-        print(f"  ~ {alias}: no provider serves it, skipped", file=sys.stderr)
-        continue
-    catalog[alias] = chain
-    primary = chain[0]
-    lines.append(f"  - model_name: {alias}")
-    lines.append(f"    litellm_params: {deployment(primary, m[primary])}")
-    for p in chain[1:]:
-        lines.append(f"  - model_name: {alias}@{p}")
-        lines.append(f"    litellm_params: {deployment(p, m[p])}")
-    if len(chain) > 1:
-        fallbacks.append((alias, [f"{alias}@{p}" for p in chain[1:]]))
-
-for canon, alias in CANONICAL.items():
-    if alias not in catalog:
-        continue
-    chain = catalog[alias]; p = chain[0]
-    lines.append(f"  - model_name: {canon}")
-    lines.append(f"    litellm_params: {deployment(p, ALIASES[alias][p])}")
-    if len(chain) > 1:
-        fallbacks.append((canon, [f"{alias}@{q}" for q in chain[1:]]))
-    catalog[canon] = chain
-
-# --- Per-route context windows, from the first provider in each chain that
-# publishes one. A pinned hop (`alias@provider`) keeps its base alias's number:
-# it is a provider fix, not a different model, and the windows a provider
-# reports for the same model agree where they overlap at all.
-windows = {}
-for alias in catalog:
-    base = CANONICAL.get(alias, alias)   # canonical ids borrow their alias's data
-    w, mx, src = None, None, "default"
-    for p in catalog[alias]:
-        mid = ALIASES.get(base, {}).get(p)
-        v = ctx.get(p, {}).get(mid) or {}
-        if v.get("context"):
-            w = v["context"]
-            mx = v.get("max")
-            src = p if p != "ollama" else f"ollama {v.get('kind', '')}".strip()
-            break
-        # else: skip to the next hop (default 200000/64000, source labelled below)
-    windows[alias] = {"context_window": w, "max_tokens": mx, "source": src}
-    if src == "ollama trained":
-        print(f"  ~ {alias}: {w} is the GGUF trained max, no num_ctx set — the"
-              " daemon may serve less (it truncates silently)", file=sys.stderr)
-os.makedirs(f"{HOME}/.local/state/litellm", exist_ok=True)
-json.dump(windows, open(f"{HOME}/.local/state/litellm/contexts.json", "w"), indent=2)
-
-# --- Free last resort, ranked from the live ollama inventory, not hardcoded.
-# When every paid hop in a chain is spent (zenmux, opencode and openrouter all
-# answered 402 no-credit on 2026-09-01) the chain's tail is a free ollama
-# model, so the client degrades to free instead of failing. Which one is
-# decided per job: heavy chains get the largest model the daemon knows
-# (parameter_count from /api/show — cloud models report 0 bytes in /api/tags,
-# so disk size cannot rank them), light chains (haiku/mini — titles, short
-# background work) get the smallest non-embedding model for latency. The full
-# ranking is written to state so the pick is auditable.
-free_ranked = sorted(
-    ((n, (ctx.get("ollama", {}).get(n) or {}).get("params") or 0)
-     for n in avail.get("ollama", set()) if "embed" not in n),
-    key=lambda kv: -kv[1])
-alias_of = lambda n: n.replace(":", "-")
-strongest = alias_of(free_ranked[0][0]) if free_ranked else None
-fastest = alias_of(free_ranked[-1][0]) if free_ranked else None
-light_job = lambda a: "haiku" in a or "mini" in a
-
-if free_ranked:
-    os.makedirs(f"{HOME}/.local/state/litellm", exist_ok=True)
-    json.dump({"ranked": [[n, p] for n, p in free_ranked],
-               "strongest": strongest, "fastest": fastest},
-              open(f"{HOME}/.local/state/litellm/free-fallbacks.json", "w"), indent=2)
-
-lines += ["", "litellm_settings:", "  drop_params: true", "  num_retries: 2",
-          "  request_timeout: 600", "", "router_settings:", "  fallbacks:"]
-for name, fb in fallbacks:
-    pick = fastest if light_job(name) else strongest
-    if pick and pick not in fb:
-        fb = fb + [pick]
-    lines.append(f"    - {name}: [{', '.join(fb)}]")
-lines += ["", "general_settings:", "  master_key: os.environ/LITELLM_MASTER_KEY", ""]
-
-open(f"{HOME}/.config/litellm/config.yaml", "w").write("\n".join(lines))
-os.makedirs(f"{HOME}/.local/state/litellm", exist_ok=True)
-json.dump(catalog, open(f"{HOME}/.local/state/litellm/catalog.json", "w"), indent=2)
-
-# pi reaches the same routes through a `litellm` provider. Regenerated here so
-# one catalogue drives both clients; the key is `!litellm-env`, never a copy.
-pi_models = f"{HOME}/.pi/agent/models.json"
-try:
-    cfg = json.load(open(pi_models))
-except Exception:
-    cfg = {}
-cfg.setdefault("providers", {})["litellm"] = {
-    "name": "LiteLLM (local)",
-    "baseUrl": "http://127.0.0.1:4000/v1",
-    "api": "openai-completions",
-    "apiKey": "!litellm-env",
-    "models": [
-        # No window resolved for an alias keeps the old flat 200000/64000 — but
-        # `source: default` in contexts.json says that is what it is, so nothing
-        # downstream can mistake the fallback for a measurement.
-        {"id": a, "name": f"{a} ({' → '.join(c)})", "reasoning": True,
-         "input": ["text", "image"],
-         "contextWindow": (windows.get(a) or {}).get("context_window") or 200000,
-         "maxTokens": (windows.get(a) or {}).get("max_tokens") or 64000,
-         "cost": {"input": 0, "output": 0, "cacheRead": 0, "cacheWrite": 0}}
-        for a, c in catalog.items()
-    ],
-}
-# Write through a temp file in the same dir and rename: pi re-reads models.json
-# on every /model open, and a plain truncate-and-write let it observe a
-# half-written file (seen 2026-08-25 as "Failed to resolve API key"). rename(2)
-# within one filesystem is atomic, so a concurrent reader sees old or new.
-tmp = pi_models + ".tmp"
-with open(tmp, "w") as fh:
-    json.dump(cfg, fh, indent=2)
-os.replace(tmp, pi_models)
-print(f"  pi: {len(catalog)} models -> {pi_models}", file=sys.stderr)
-print(f"\nwrote config.yaml: {len(catalog)} routes", file=sys.stderr)
-for a, c in catalog.items():
-    w = windows.get(a) or {}
-    ws = f"{w['context_window']:,}" if w.get("context_window") else "200000 (default)"
-    print(f"  {a:28} {' -> '.join(c):42} ctx {ws:14} [{w.get('source', 'default')}]",
-          file=sys.stderr)
diff --git a/home/dot_local/bin/executable_litellm-up b/home/dot_local/bin/executable_litellm-up
deleted file mode 100755
index 516f66d..0000000
--- a/home/dot_local/bin/executable_litellm-up
+++ /dev/null
@@ -1,7 +0,0 @@
-#!/usr/bin/env bash
-# litellm-up — start the local LiteLLM proxy (foreground).
-set -euo pipefail
-. "$(dirname "$(command -v litellm-env)")/litellm-env"
-
-: "${OPENROUTER_API_KEY:?no openrouter credential in ~/.pi/agent/auth.json}"
-exec litellm --config "$HOME/.config/litellm/config.yaml" --port "$LITELLM_PORT" "$@"
diff --git a/home/dot_local/bin/executable_llm-quota b/home/dot_local/bin/executable_llm-quota
deleted file mode 100755
index c83e4a5..0000000
--- a/home/dot_local/bin/executable_llm-quota
+++ /dev/null
@@ -1,161 +0,0 @@
-#!/usr/bin/env python3
-"""llm-quota — what's left to spend, per provider.
-
-  llm-quota                     show the table
-  llm-quota --json              machine-readable (the nu module reads this)
-  llm-quota refresh             re-fetch every provider that exposes a balance API
-  llm-quota set <prov> <remaining> [--limit N] [--unit credits|tokens|requests]
-
-Only OpenRouter publishes a balance (/api/v1/credits). Zenmux returns 404 on
-every documented balance path, and opencode-zen exposes none, so those are
-recorded by hand — `set` is how you tell this file what the dashboard said.
-A manual figure is a reading, not a live number: its age is shown so a stale
-one is never mistaken for current.
-"""
-import json, os, subprocess, sys, urllib.request
-from datetime import datetime, timezone
-
-STATE = os.path.expanduser("~/.local/state/litellm/quota.json")
-AUTH = os.path.expanduser("~/.pi/agent/auth.json")
-PROVIDERS = ["zenmux", "opencode", "opencode-go", "openrouter", "ollama"]
-
-
-def load():
-    try:
-        return json.load(open(STATE))
-    except Exception:
-        return {}
-
-
-def save(d):
-    os.makedirs(os.path.dirname(STATE), exist_ok=True)
-    json.dump(d, open(STATE, "w"), indent=2, sort_keys=True)
-
-
-def key(prov):
-    try:
-        e = json.load(open(AUTH)).get(prov) or {}
-    except Exception:
-        return ""
-    return e.get("key") or e.get("access") or ""
-
-
-def now():
-    return datetime.now(timezone.utc).isoformat(timespec="seconds")
-
-
-def refresh(state):
-    """Fetch what can be fetched. Never overwrite a manual entry with a failure."""
-    # OpenRouter: total_credits minus total_usage is the real remaining balance.
-    try:
-        r = urllib.request.Request("https://openrouter.ai/api/v1/credits",
-                                   headers={"Authorization": "Bearer " + key("openrouter")})
-        d = json.load(urllib.request.urlopen(r, timeout=20))["data"]
-        total, used = float(d["total_credits"]), float(d["total_usage"])
-        state["openrouter"] = {"remaining": round(total - used, 4), "limit": total,
-                               "unit": "credits", "source": "auto", "updated": now()}
-    except Exception as e:
-        print(f"llm-quota: openrouter refresh failed: {e}", file=sys.stderr)
-
-    # Ollama is local: free and unmetered as long as the daemon answers.
-    try:
-        urllib.request.urlopen("http://127.0.0.1:11434/api/tags", timeout=4)
-        state["ollama"] = {"remaining": None, "limit": None, "unit": "free",
-                           "source": "auto", "updated": now()}
-    except Exception:
-        state["ollama"] = {"remaining": 0, "limit": None, "unit": "free",
-                           "source": "auto", "updated": now(), "note": "daemon down"}
-    return state
-
-
-def reachable(prov):
-    """Does the credential actually authenticate right now?"""
-    bases = {"zenmux": "https://zenmux.ai/api/v1",
-             "opencode": "https://opencode.ai/zen/v1",
-             "opencode-go": "https://opencode.ai/zen/go/v1",
-             "openrouter": "https://openrouter.ai/api/v1"}
-    if prov not in bases:
-        return None
-    try:
-        r = urllib.request.Request(bases[prov] + "/models",
-                                   headers={"Authorization": "Bearer " + key(prov)})
-        urllib.request.urlopen(r, timeout=15)
-        return True
-    except Exception:
-        return False
-
-
-def age(ts):
-    try:
-        delta = datetime.now(timezone.utc) - datetime.fromisoformat(ts)
-    except Exception:
-        return "?"
-    s = int(delta.total_seconds())
-    if s < 3600:
-        return f"{s // 60}m ago"
-    if s < 86400:
-        return f"{s // 3600}h ago"
-    return f"{s // 86400}d ago"
-
-
-def table(state, checked):
-    w = max(len(p) for p in PROVIDERS) + 2
-    print(f"{'provider':<{w}}{'remaining':>13}  {'limit':>9}  {'source':<7}{'updated':<10}auth")
-    print("-" * (w + 46))
-    for p in PROVIDERS:
-        e = state.get(p, {})
-        rem, lim = e.get("remaining"), e.get("limit")
-        unit = e.get("unit", "")
-        rs = "free" if unit == "free" and rem is None else (
-            "—" if rem is None else f"{rem:,.2f}" if isinstance(rem, float) else f"{rem:,}")
-        ls = "—" if lim is None else f"{lim:,.0f}"
-        ok = checked.get(p)
-        auth = "—" if ok is None else ("ok" if ok else "FAIL")
-        flag = "  ⚠" if (isinstance(rem, (int, float)) and rem <= 0) or ok is False else ""
-        print(f"{p:<{w}}{rs:>13}  {ls:>9}  {e.get('source','—'):<7}"
-              f"{age(e['updated']) if e.get('updated') else '—':<10}{auth}{flag}")
-    note = [p for p in PROVIDERS if state.get(p, {}).get("note")]
-    for p in note:
-        print(f"  note: {p} — {state[p]['note']}")
-
-
-def main():
-    a = sys.argv[1:]
-    state = load()
-
-    if a and a[0] == "set":
-        if len(a) < 3:
-            sys.exit("usage: llm-quota set <provider> <remaining> [--limit N] [--unit U]")
-        prov, rem = a[1], a[2]
-        rest = a[3:]
-        e = {"remaining": float(rem) if "." in rem else int(rem),
-             "limit": state.get(prov, {}).get("limit"),
-             "unit": state.get(prov, {}).get("unit", "credits"),
-             "source": "manual", "updated": now()}
-        for i, tok in enumerate(rest):
-            if tok == "--limit" and i + 1 < len(rest):
-                e["limit"] = float(rest[i + 1])
-            if tok == "--unit" and i + 1 < len(rest):
-                e["unit"] = rest[i + 1]
-        state[prov] = e
-        save(state)
-        print(f"llm-quota: {prov} = {e['remaining']} {e['unit']} (manual)")
-        return
-
-    if a and a[0] == "refresh":
-        state = refresh(state)
-        save(state)
-
-    checked = {}
-    if "--json" not in a:
-        for p in PROVIDERS:
-            checked[p] = reachable(p)
-
-    if "--json" in a:
-        print(json.dumps(state, indent=2, sort_keys=True))
-        return
-    table(state, checked)
-
-
-if __name__ == "__main__":
-    main()
