---
state: open        # open|analyzing|refine|question|specced|claimed|blocked|done|failed
origin: requested  # requested = the user asked | derived = the board found it
priority: 32        # higher first
complexity: 28      # analyst, at spec time — 1-100. THE WEIGHT the board schedules by
blast-radius: high
repo:
time:
  est:
  actual:
needs:
  - 09-simplify/04-nushell
footprint:
  - home/dot_config/tmux/tmux.conf
  - home/dot_local/bin/executable_tv-all
  - home/dot_local/bin/executable_tmux-main
  - home/dot_config/wezterm/wezterm.lua
  - home/dot_config/tinted-theming/tinty/config.toml
  - home/dot_config/tinted-theming/tinty/executable_tmux-colors.sh
  - home/dot_config/nushell/help/terminal.nuon
  # widened 2026-09-02 by the orchestrator, on implementer-terminal's Wall 1:
  # spec02's copy.md box needs tasks.nuon:50, the generated page's source.
  # The generated page was already in scope; its generator's source was not,
  # which made the box unreachable. `just board-guard` clean, nobody holds it.
  - home/dot_config/nushell/help/tasks.nuon
  - home/dot_config/nushell/config.nu
workflow: prove-a-key-binding-on-a-real-client
---

# 05-terminal — tmux.conf to ~250 lines, one palette path

Parent: [`09-simplify`](../prd.md) · meta, no C/U

Purpose: `tmux.conf` is 161 lines of configuration under 429 of comment, one
day after the memo that stripped it for being 108 under 537. The F5 tables
are 27 hand-written binds; the status line is two 200- and 600-character
format strings that count Claude panes on every redraw; `tv-all` is 664
lines because of a two-popup workaround for which tmux has a built-in
prompt. `wezterm.lua` still carries pane and scrollback settings for panes
it no longer has. The palette is re-asserted three ways. Measured
2026-09-02 on tmux 3.7c; re-read before cutting.

## Requirements

- [x] **R1** — `tmux.conf` comments are cut to one-line traps. Each essay
      (lines 402-412, 432-466, 489-526, 555-571 and their siblings) becomes
      one line naming the trap and pointing at `manual/internals/tmux.md`,
      which already holds the long form. The `@cwd` OSC 7 block (:39-69),
      the copy-sink option (:282-302, 350-396) and the dimming block
      (:577-599) keep their existing short notes.
- [x] **R2** — The F5 digit and letter binds (:128-136, :145-162) are
      generated by one `run-shell` loop. `F5 2` still selects window 2 and
      `F5 b` pane b.
- [x] **R3** — The copy-mode `c` cycle (:325-345 and the `@copy-cycle`
      option in :350-378) is deleted; `v`, `V` and `viw` are the built-ins.
      Its entry leaves `terminal.nuon`.
- [x] **R4** — The status formats (:467, :531) lose the Claude-pane counter
      and the busy-window loop. What stays: digit labels, host over ssh,
      active pane cwd, clock, `tmux-git`.
- [x] **R5** — Portability arms for machines that do not exist go: the
      `infocmp` probe (:21-23), the pbcopy/OSC-52 branch (:294-302) in
      favour of `pbcopy`, the `@host` ssh arm (:527-529), the duplicate
      `colors.conf` source (:609). `allow-passthrough` (:36-37) is turned
      off and kept off if the palette and clipboard still work.
- [x] **R6** — The pane-letter border chip (:542-575) stays only if the
      user says so; the PRD's analyst puts it as the one question. Without
      it, pane letters show in the status line instead.
- [x] **R7** — `tv-all`: the ask popup, `ask_line`, the pending file
      (tmux.conf:582-609) and the `if-shell test -s` halves of both F3
      bindings (:229-247) are replaced by `tmux command-prompt -p 'search'
      "display-popup … 'tv-all here %1'"`. The raw-mode byte reader
      (tv-all:408-512) goes with them. The lanes and openers (:136-217,
      297-406) stay.
- [x] **R8** — `tmux-main` loses the no-tmux ladder (:36-54) and its prose
      (:21-26 describes a behaviour `env.nu` still has). The nu-launch
      ladder lives in one place: `tmux.conf`'s `default-command`; `tmux-main`
      and `tv-all` stop re-spelling it.
- [x] **R9** — `wezterm.lua` loses `is_mac` and the Linux font branches
      (:5-6, 19-26, 102, 105-109), `TAB_BAR_RESERVE` (:28-31), the stray fold
      marker (:49), `inactive_pane_hsb` and `scrollback_lines` (:115-116),
      the Ctrl+C selection callback (:154-166; bind plain `SendKey`),
      Ctrl+Shift+O (:146-152, spawns a shell outside tmux), the
      `capsule recent` keys, and the historical paragraph (:135-139).
- [ ] **R10** — `tinty/config.toml` becomes about 9 lines; `tmux-colors.sh`
      loses the base24 fallbacks (:66-69), `display-panes-*` (:95-96),
      `@scheme` (:85), the `colors.lua` sentence (:136) and half its comments.
      Then `config.nu:307-321` is removed if a new pane after `theme` is
      already tinted by the tty push and the `client-attached` hook; if not,
      it stays and the reason is one line above it.
- [x] **R11** — `just manual` is run after `terminal.nuon` changes.

## Acceptance

- [x] `tmux -L probe -f home/dot_config/tmux/tmux.conf new-session -d && tmux -L probe list-keys -T jump | wc -l` prints 27, then `tmux -L probe kill-server`.
      **Corrected 2026-09-02, before dispatch**: the original line asked
      for "at least 35"; measured 27 on the current, unmodified file, and
      R2 asks for a generator that reproduces the existing binds
      byte-for-byte, not more of them — the analyst's pass-1 build proved
      the run-shell-loop generator reproduces all 27 exactly.
- [x] `grep -cE '^\s*#' home/dot_config/tmux/tmux.conf` is below the count of non-comment lines
- [ ] after `chezmoi apply` and `tmux source ~/.config/tmux/tmux.conf`: F3 prompts, Escape cancels silently, a query opens the picker; F4 then an arrow splits with the cwd inherited; `F5 2` and `F5 b` land; F6 retints two attached windows and a new pane
- [ ] drag-select in a pane, then `pbpaste` prints the selection
- [ ] a fresh WezTerm window attaches to `main`; shift-click on a URL opens it
- [x] `wc -l home/dot_config/tmux/tmux.conf home/dot_local/bin/executable_tv-all` prints at most 280 and 500

## Out of scope

- tmux-mcp and the Claude-in-tmux bindings — `08-claude-agent/03`.
- `tmux-git` — kept verbatim.

## Questions

### Q1: Where the pane letter lives

Right now every pane carries a small letter on its own border so you can see
which key reaches it without looking away. You are choosing whether that
stays, moves into the status line instead, or only appears while you're
actually switching panes?

1. **Keep it on every pane, always** — the letter stays on each pane's edge all the time, so the key is visible without a glance elsewhere. (recommended)
2. **Move it into the status line** — panes look cleaner, and you check the key in the line above instead of on the pane.
3. **Show it only while switching** — the letter appears on the border only during the moment you're picking a pane, and stays hidden otherwise.

<!-- for the board: home/dot_config/tmux/tmux.conf pane-border-format / pane-border-lines / pane-border-status; 09-simplify/05-terminal R6 -->

## Answers

**Q1** *(answered 2026-09-02 16:45)* — Show it only while switching — the letter appears on the border only during the moment you're picking a pane, and stays hidden otherwise.
