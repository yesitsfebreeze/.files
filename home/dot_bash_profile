# Login shells read .bash_profile (if present) instead of .profile.
# Defer to .profile so PATH (~/.local/bin) and .bashrc are set up the same
# way for login shells in WezTerm.
[ -f "$HOME/.profile" ] && . "$HOME/.profile"
