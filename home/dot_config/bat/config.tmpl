# bat config. Theme managed centrally in home/.chezmoidata/theme.yaml;
# "gruvbox-dark" is a built-in bat theme (no fetch needed).
--theme="{{ .theme.names.bat }}"
--style="numbers,changes,header"
--paging=auto
