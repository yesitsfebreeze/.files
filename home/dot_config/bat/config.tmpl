# bat configuration.
# Theme is managed centrally in home/.chezmoidata/theme.yaml.
# "gruvbox-dark" is a built-in bat theme (no fetch/cache rebuild required).
--theme="{{ .theme.names.bat }}"
--style="numbers,changes,header"
--paging=auto
