<!-- GENERATED by scripts/generate-manual.mjs — edit the .nuon surfaces, not this file. -->

# See what is in a directory

> The decorated `ls`, its short forms, and real directory sizes.

A listing prints itself after every move, so most of the time you do not run one. When you do, `ls` returns structured data — a real table you can filter with `| where`, not text you have to parse.

## `ls`

**List a directory as structured, decorated data**

*shell*

Plain `ls`. Rows are sorted by type then modified time, so directories group together and the freshest thing sits nearest the prompt, and every row except a directory carries a Nerd Font icon — one for its extension, or a generic one where the extension is unknown or unmapped; directory rows show none. It is still a nushell table: `ls | where size > 1mb | sort-by modified` works.

> **Why it is this way**
>
> The builtin is captured as `core-ls` before being shadowed, because an alias target binds at parse time — without the capture the wrapper would call itself.

See also: [`l / ll / la`](./listing.md#l-ll-la) · [`ls -D`](./listing.md#ls-d)

## `l / ll / la`

**List short, long, or including dotfiles**

*shell*

`l` is the quick one, `ll` the long form, `la` includes hidden files. `la` is also what runs by itself after every directory change.

See also: [`ls`](./listing.md#ls)

## `ls -D`

**List with real recursive directory sizes**

*shell*

Add `-D` to swap each directory's inode size for what it actually occupies on disk. One `du` spawn does the whole listing.

> **Why it is this way**
>
> Opt-in only, and it stays that way: a single `node_modules` would stall every listing in the tree if this were the default. It also cannot be spelled `du -sb` — macOS `du` has no `-b`, and the old implementation discarded the error, so every directory silently kept its inode size.

See also: [`ls`](./listing.md#ls)

## `y`

**Open the yazi file manager**

*shell*

`y` is `yazi`, the terminal file manager: move with the arrows or `hjkl`, `Enter` opens a file or descends into a directory, `q` quits back to the shell.
