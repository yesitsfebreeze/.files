// §head home/dot_config/wp-stat-overlay/wallpaper/js/look.js:30-34 target
// §sig function target(name)
    var v = isTyping ? typing[name] : idle[name];
    if (typeof v === "undefined") v = idle[name];
    return v;
// §foot home/dot_config/wp-stat-overlay/wallpaper/js/look.js target