# Round — closed out. Nothing is claimed, nothing uncommitted, one node specced and ready.

## Where to pick up
`g1-verify-still-red-on-just-gates` is **specced, 3 specs, 24 open boxes,
c=40 mid** — the next dispatch. Its probe code is committed. Two things it was
asked and never answered, named in its specs and NOT assumed:
- **R5** — sweep `0b77a71`'s 36 files for a THIRD unmaintained gate. Two were
  found by accident by two nodes on one day; nobody has enumerated the commit.
- **R1** — do `dot_local` and `litellm` belong under `home/` at all? Argue it;
  do not declare whatever appeared, which is how the decision gets skipped.

## Established (cite, never re-run)
- `just gate-selftest` **rc 0** and `just gates` **rc 0 / sweep rc=0**, run
  detached 2026-08-28. The sweep's green includes g1's fixes, now committed
- **Three checks found this round that could not fail as written**:
  02-terminal's config-field probe, tree-links' fail-closed assertion (its
  marker landed at EOF past every link — blind its whole life), and
  shell-init's S1.10 (red at `2714042`, the commit that added it)
- **`config_builder()` decides everything** for a WezTerm probe; a bare
  `return { ... }` table validates nothing. Both exit 0 — the exit code is
  never a predicate
- The litellm `create_` bug: apply → regenerate → `MM` → apply → routes gone.
  Fixed as a class at `tests/managed-config.sh:474`
- S1.10 / R4 are **fixture defects**; nothing overwrites a user's television
  config. Measured across three worktree commits
- doctor: origin 44 with no `from:`, all on `done` nodes — reconciles with the
  counted residue. `from: tbd` is the sentinel for an unestablishable origin

## Decided
- **Call the skeptic before `done` on my own work** — it caught a false
  measurement, a false tick, a false sentence and two overclaimed epic
  closures in one round.
- Never tick a box on a good argument. Two were left open until the *user*
  fixed the contract (split-entry rating rule; `from: tbd`).
- A stale contract is rewritten with the old text kept, never silently.
- A transition is confirmed **on disk**, never on a worker's report — that is
  how g1 reached `specced` after its analyst died twice.

## Asked
- All four board questions answered and landed. **Out and needing a person:**
  the **C.4 five** (holds `01-capsule`), and **H.5** (holds
  `06-help/05-agent-interface`) — the reading test needs an uncontaminated
  session, so no agent that has read this repo can grade it.

## Owed
- Dispatch an implementer on `g1-verify-still-red-on-just-gates`.
