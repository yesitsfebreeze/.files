# Round — a board-frontier drill, answered. Nothing dispatched.

## Owed
Four answers are recorded and none is acted on yet — the drill's rule is that
the board picks the tree up, not the drill. In dispatch order:

1. **`01-capsule/04-recent-workspaces`** — build the C.4 harness and run the
   five. Isolated instance at this repo's config file, real GUI keys via
   osascript, screen read with `wezterm cli --class <name> get-text`. The node
   carries the three conditions the tick must satisfy.
2. **`06-help/05-agent-interface`** — tick H.5 on the filed evidence, naming
   the orchestrator as the setter-up and the second run as the grade. This
   unblocks six nodes: `04-drift-check`, `06-help`, `coverage`,
   `drift-check-terminal-surface`, `finish-line`, `00-delivery`.
3. **`05-platform/01-deploy-mechanism`** — build R7, then run what a scratch
   `HOME` plus a Linux container can prove, listing the macOS-only steps it
   could not.
4. **`00-delivery/corrections/questions-check-red-only-on-history`** — new,
   `open`, `origin: requested`. R2 lands in `~/dev/infra/pearde`, not here.
5. **`g1-verify-still-red-on-just-gates`** — still specced, still the largest
   thing ready (w85). Unchanged by this round.

## Asked
**Nothing is out with the user.** The drill's frontier is empty: all four
forks answered, and no answer opened a question that was not already asked.

Two things the round settled that were *not* forks and are recorded where they
belong rather than carried here: `just cutover` is a visible unmade decision
that no node owns (`04-recent-workspaces` `## Answers`), and the derived-work
tripwire is tripped at **3 derived vs 2 requested in-flight** — the same shape
as the 12/12 that parked `done-node-proof-gate` on 2026-08-25, which is why
reopening that node was dropped from the round rather than put to the user.

## Established (cite, never re-run)
- **C.4 was never blocked on a human.** Measured 2026-08-29: the picker
  bindings are at `home/dot_config/wezterm/wezterm.lua:1213` and `:1218` and
  are **absent** from `~/.config/wezterm/`; `show-keys --lua` counts 0
  `SpawnCommandInNewTab`; `command -v capsule` is empty; `~/.cache/capsule/`
  does not exist; `chezmoi source-path` is `/Users/feb/dev/.files/home`. The
  blocker is that `just cutover` has not run.
- **A GUI keystroke can be injected and the screen read back.** `osascript`
  System Events returns rc=0 (Accessibility granted), so a real key event
  reaches the binding layer — `wezterm cli send-text` does **not**, it pastes
  into the pane and never touches the bindings. `get-text` reads any pane;
  `wezterm start --always-new-process --class <name>` is restartable without
  killing the driving session; Docker 29.4.0 is up.
- `pearde questions check` reports **29**, not 28, and all 29 are on `done`
  nodes. Split 9 `mode: hitl` / 6 answers-without-questions / 14 malformed.
- H.5 has two filed uncontaminated passing runs, the second post-`0f9f635`.
  The independence on file is the **subject's**, never the grader's.

## Decided
- **A fact is never a fork.** Q2 came back as "cant you just create a new
  wezterm instance yourself?" — a question, not an answer. Measuring it
  changed the fork's premise entirely and the round re-put it informed. Asking
  the user to decide a thing that could be looked up is the failure this
  avoids.
- **Do not overrule a tripwire on the orchestrator's recommendation.**
  `done-node-proof-gate` stayed out of the round for that reason alone.
- **The heading is the claim, including in what I write.** Recording the
  05-platform answer produced an `## Answers` with no `## Questions` above it
  — the exact defect the new node is about. Renamed, not left.
