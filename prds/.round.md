# Round — eight nodes landed; the round's own measurement was refuted and corrected

## Established
- **`config_builder()` is the whole difference** — it installs a validating
  `__newindex`; a bare `return { ... }` table installs none. Clean 791 stdout /
  0 stderr; + bogus key 0 stdout / 350 stderr. **Both exit 0** · 11:58Z
- My 11:48Z claim that this build silently ignores unknown keys was **false**
  (bare-table probe). Skeptic refuted it; I reproduced the refutation · 11:58Z
- The wezterm gate's harvest reads `config.<field>` from **prose**, so it went
  red against the paragraph describing its own red. Per-line `NOT-A-FIELD`
  marker; RED 1 + RED 2 still bite, and a marked line does not blind the
  next · 12:45Z
- `agent-overview-derived-tools`' premise was **stale** — the idioms title has
  read `Search with rg, find with fd, pick with tv` since `0f9f635`
  (2026-08-24), `shell.nuon:394` · 13:00Z
- That shipped fix was **held by nothing**: revert the title and
  `discovery_ok` stays green (it reads the expected title out of the same
  corpus in the same run). Now gated — `help-agent.sh` 90 → **100 PASS**,
  CF6 asserts both halves · 14:10Z
- S.10 census: class B = **0**; but `--validate` **cannot** see a mispairing —
  its two assertions are independent set-coverage checks · 13:00Z
- doctor live debt was **2 items** of 45+30; residue on `done`: 44 no-`from:`,
  9 malformed rounds, 8 stale `mode: hitl` · 13:50Z
- `tree-links.sh:217,224` pin `exempt 9 links in 4 files`; live is **13 in 5**.
  That is why `gates/selftest.sh` exits 1, and why `G.1`'s own verify
  (`just gate-selftest && just gates`) fails while `G.1` is `done` · 13:55Z

## Decided
- **Call the skeptic before `done` on my own work** — it paid four times in one
  round.
- Epics get a real acceptance box + a verify that goes red, not a note arguing
  they need neither.
- Two boxes left **open** rather than ticked on a good argument: 02-terminal's
  rating box (until the user added the split-entry rule) and
  `doctor-debt-live-nodes`' first box, which contradicts its own R1.
- A stale contract is **rewritten with the old text kept**, not silently
  replaced — `agent-overview-derived-tools` R1–R3.
- I set derived-tools `done` while its spec01 boxes were unrun, and reverted it
  to `claimed`. A spec's boxes are the evidence; the PRD's prose is not.

## Asked
- D.3 closures · **A** · landed `5016922`
- Manual gates · user runs the **C.4 five** · **still out**
- Rating-rules gap · **A**, split-entry rule · landed `61dec66`
- derived-tools Q1 · **A**, keep the title, gate it · implementer running
- **`doctor-debt-live-nodes` box-1 vs R1 contradiction · NOT YET PUT**

## Owed
- Put the doctor-debt contradiction to the user; collect the two live workers
  (tree-links analyst, derived-tools implementer).
