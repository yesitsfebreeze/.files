# Round — shell-y correction done (ad6a374); 02-nvim-plugin analyst the last flight.

Board at this collect (2026-08-31): done 191/195 · 92% by est · derived
100/100 · open 2 · deferred 1. `workers=3`; two slots open.

## In flight (claims live, `claim:` on each PRD)
1. **`08-claude-agent/02-nvim-plugin`** — `analyzing`, claim
   `analyst-nvim-plugin`, analyst still out. Everything else on the board
   gates on or follows it.

## Collected
- **`08-claude-agent/01-tmux-mcp`** — `done`, commit `185585e`, record
  `41fd04f`. DONE report after one infra-stall resume: 12 boxes closed with
  quoted measurements; collect re-ran the verify blocks itself (gate
  `tests/tmux-mcp.sh` exit 0 ×2, selftest 19 PASS 0 FAIL). Two measured
  deviations kept on the record in the specs, not absorbed: the release
  assets are `tmux-mcp_$TRIPLE.tar.gz` (underscore; the spec's hyphens 404
  on all four triples), and `claude mcp add` onto an existing entry exits
  non-zero (the script warns, the overwrite is by value). Deviations in the
  gate's fixtures: tmux sockets pinned under /tmp — a `${TMPDIR}`-deep
  socket path exceeds `sun_path`'s 104 bytes ("File name too long",
  measured; spec03 header).
- **`00-delivery/corrections/shell-y-entry-missing-review-row`** — `done`,
  commit `ad6a374`, record `98000d7`. The analyst's pass one proved both
  boxes, so specced warned "2 of 2 boxes already ticked"; claimed and
  collected straight through — collect re-ran `nu tests/help-content-model.nu`
  (102 entries, ok) and the gate went green. One row appended to
  `use-review.nuon`, digest re-derived not copied; `--widen` took the row
  file (its edit predates the impl claim — pass one wrote it). Fourth
  instance of the one-row-review-correction job, per the report — still not
  a workflow. The row honestly records that its subject (the `y`→yazi help
  entry) is the concurrent lane's uncommitted work: a reword by that lane
  goes red by design.
- **`08-claude-agent/03-tmux-config`** — `done`, commit `7afecbb`, record
  `137fbdd` (earlier this round; details below in Decided history).
- **Out-of-scope finding, the derived call:** `wave-status --validate`
  reports `tests/tmux-claude-config.sh` unreferenced — the known
  board-wide no-`task:`-id defect (recorded in
  `07-multiplexer/01-session-and-windows/specs/spec03.md`), no new filing.
  `tests/tmux-mcp.sh` got a wave-4 row (gate column only — this node has no
  `task:` id either; path-keyed re-keying remains the recommendation).

## The [~] ruling (decided this round, cite it next time)
`impl-tmux-mcp` closed its 12 acceptance boxes as `- [~]` (met-against-
stub, per the repo's AGENTS.md wording). Pearde's `acceptance()` counts only
`[ xX]` and `collect` refuses a spec set it counts 0/0 — a spec written
entirely in `[~]` can never collect. Re-ticked 12× `[~] → [x]` as the
orchestrator's edit after reading each quoted measurement: every box ran
against the real thing (live registration end-to-end from empty, real
release tarball fetched and installed, real gate runs), the staging
(scratch HOME, PATH stubs, a slice run because a full install.sh re-enters
brew) is the box's own documented method, and collect re-proved the claims
with its own runs. **The repo's AGENTS.md `[~] means met-against-stub` and
pearde's `[~] means contract term withdrawn` disagree; on this board a box
with real evidence is `[x]`, and `[~]` is for a check run against genuinely
stubbed infrastructure.** A correction against AGENTS.md would make this a
documented rule instead of a round-file one.

## In flight (claims live, `claim:` on each PRD)
1. **`08-claude-agent/02-nvim-plugin`** — `analyzing`, claim
   `analyst-nvim-plugin`, analyst still out.
2. **`00-delivery/corrections/shell-y-entry-missing-review-row`** —
   `analyzing`, claim `analyst-shell-y-row`, analyst dispatched.

On each return: SPECCED → `pearde specced <prd> --blast <x>`, then hand to
its implementer in the same round. REFINE → `pearde refine < report`.
QUESTION → the questions go to the user next round, per drill.

## Queued, not claimable this round
- **`08-claude-agent/04-help-entries`** — gated on 02-nvim-plugin.
- **`08-claude-agent`** (epic) — gates on 04, 02 (01 and 03 are done).
- **Correction owed before 04-help-entries claims:** the
  settings.json→claude.json destination error and the
  `CLAUDE_CODE_ALTGR_AS_TEXT` quirk, found by this round's 01-tmux-mcp and
  03-tmux-config reports. Two rows, one correction PRD — file it against
  04-help-entries when its claim opens, per the derived call below.

## Parked
- **`00-delivery/corrections/done-node-proof-gate`** — parked. Who un-parks
  it and on what evidence is not recorded here.

## Carried from the previous round (not re-verified this round)
- **I owe two declaring comments** when the retired-phrases spelling lands:
  `07-multiplexer/01-session-and-windows/specs/spec03.md` and its
  `probe/notes.md`. Nothing scrubbed either side.
- **`07-multiplexer` carries no `task:` id on any node** — recorded in
  `01-session-and-windows/specs/spec03.md`; re-keying recommendation lives
  in `dotfiles-9e`'s `00-delivery/wave-registry-keying`.
- **Ownership split:** `00-delivery`, `05-platform`, `01-capsule` were
  claimed by session `dotfiles-9e`. The round file that said so predates the
  multiplexer cutover; treat as historical, confirm before acting on it.

## Decided this round
- `pearde brief` on a held PRD is the normal case once `claim` has run: pass
  `--role analyst --force` on the brief, since the claim the loop prescribes
  is what makes plain `brief` refuse.
- **01-tmux-mcp's five findings, the derived call** (per @@derived):
  1. PRD body claimed installation+registration already true — it was not;
     pass one made it true. Fixing the body is the orchestrator's edit at
     collect, not a correction PRD: the ship-work is already spec'd.
  2. PRD's contract names `~/.claude/settings.json` as MCP destination — dead
     in Claude Code 2.1.251; user scope is `mcpServers` in `~/.claude.json`.
     Changes what ships for **nothing yet requested** that touches MCP
     config, but 08-claude-agent/04-help-entries will document it — if the
     help entries say settings.json they are wrong. **Correction to file**
     against 04-help-entries before it claims. Same story as (1) for the PRD
     body: orchestrator's edit at collect.
  3. `execute-command` is a guaranteed-hang on nushell panes (parser error in
     its wrapper before the sentinel ever lands) — pinned in spec03 as a
     gate stage, which is the instrument; a memo is unnecessary because the
     gate carries it.
  4. `env -u CLAUDE_CONFIG_DIR` load-bearing in registration — encoded in
     spec02; noted for 04-help-entries too.
  5. Release-rung job pattern (release-rung binary + registration +
     self-testing gate) seen twice — tinty, now tmux-mcp. Not a workflow
     until a third instance; recorded here, not filed.

- **03-tmux-config's findings, the derived call** (per @@derived):
  1. Material for **04-help-entries**: the claude binary's
     `CLAUDE_CODE_ALTGR_AS_TEXT` quirk (off|force|auto, WezTerm-session
     aware) surfaced on the same decode path. Folds into the same correction
     this round already owes 04-help-entries (the settings.json/claude.json
     one from 01-tmux-mcp) — one correction PRD when filed, two rows.
  2. `tests/tmux-claude-config.sh` not registered in `gates/waves.tsv` — the
     known board-wide no-`task:`-id defect, already recorded in
     `07-multiplexer/01-session-and-windows/specs/spec03.md`; unchanged here,
     no new filing (a derived PRD against a derived PRD).
  3. Fixture traps recorded in `probe/notes.md` E5 — cat-based pane collector
     lies (use `stty raw` + `dd bs=1`); `printf '%b'` inside an unquoted
     heredoc becomes `%%b`. Probe-record facts, nothing to file.
  4. Pass-one code stands in `home/dot_config/tmux/tmux.conf` (append-only
     section) and `tests/tmux-claude-config.sh` — the implementer continues
     it; the uncommitted `y → yazi` alias and wezterm.lua rewrite in the
     tree are other work's and untouched.

## Established (cite, never re-run)
- Everything in the previous round's file stands — the `chk "label $(cmd)"`
  false-PASS lint, the `set -e -o pipefail` verify-block traps, the
  `PROV_BINS`/`PKGS` coupling, the `tests/nvim-small-plugins.sh` group count
  (now six, rc=0, 151 PASS 0 FAIL). The prior text was folded here rather
  than deleted; git history has its full wording.