# Round — g1 landed; the board is split between two sessions.

## Owed
- **`00-delivery/corrections/done-nodes-with-unticked-boxes`** — `analyzing`,
  dispatched. 41 unticked boxes under six `done` `02-terminal` children; its R4
  asks whether the epic is special or the board is.
- **`00-delivery/quiet-board-sweep`** — the top of `ready` by priority and
  **not runnable now**: it needs a board with no lane in flight, and there are
  three. The scan cannot know that; don't dispatch it on the ordering alone.
- **`00-delivery/wave-registry-keying`** — `waves.tsv` keys on `task:`; 0 of 9
  `07-multiplexer` nodes carry one, and 50 of 116 `done` nodes never did.
- **`00-delivery/corrections/questions-check-red-only-on-history`** — R2 lands
  in `~/dev/infra/pearde`, which two other sessions are contending over. Hold
  until that tree is quiet; the board half (R1) is already answered as REVERSED.
- **`01-capsule`** the epic — three acceptance boxes, `verify: ""`. Box 3
  ("`git push` and SSH inside the container without re-authentication") needs a
  real remote and the user's credentials; that is a fork, not a task.
- **`05-platform`** — R7 built; the cold-`<data>/mason` run and anything naming
  a fresh machine are still unrun.

## Asked
**Out with the user and unanswered: did they sanction the tmux direction?**
`07-multiplexer` is nine PRDs, a `home/dot_config/tmux/` tree and a `tmux-main`
binary, resting on a memo a board session wrote and marked `decided` on
2026-08-29. `05-platform/02-package-provisioning/tmux-not-installed-on-the-host`
is filed `hitl` and parked on exactly that: its R1 is "establish the user
settled this, quote where; if not, stop". Nothing else is out.

## The split, agreed with dotfiles-06
**Mine:** `00-delivery`, `05-platform`, `01-capsule`.
**Theirs:** `07-multiplexer`, `06-help`.
They stay off `tests/shell-init.sh`, `tests/managed-config.sh`,
`tests/shell-litellm.sh`, `home/dot_config/litellm/`. Every commit this round
used explicit paths. Their three workers were silent 51m/71m/87m at last scan —
**not mine to sweep**, and the one I nearly wrote off as dead came back REFINE
with seven children.

## Established (cite, never re-run)
- **g1's five reds are gone.** `tests/shell-init.sh` **EXIT=0, 100 PASS / 0
  FAIL** serially; `tests/managed-config.sh` **EXIT=0, 67 PASS / 0 FAIL** on a
  clean tree. S1.10 and R4 each proven by their own counterfactual.
- **A third stale gate of the same class, caused by my own change.** `S3.10`
  filtered chezmoi status on ONE literal always-run script name; `5e7934c`
  added a second (the mason seeder). Now derived from the tree. Three for
  three, the mechanism was **a hand-kept list standing in for a property of
  the tree** — and a fourth instance is live in the wave registry.
- **R5 answered:** all 36 files of `0b77a71` enumerated; no third unmaintained
  gate traces to it. The count is two and complete.
- **R1 answered:** `dot_local` declared, narrowed to `bin`; `litellm` declared
  *pending*, routed to `managed-config` rather than silently settled.
- `just gates` on a live tree: **3550 PASS / 43 FAIL**, 23 armed reds, **none
  the subject of the node that ran it** — 18 nvim gates paired 1:1 with
  `persistence.nvim is absent`, one `undeclared: tmux`, three on an untracked
  memo.

## Decided
- **`blocked` must name its event, and the tool was right to refuse.** g1's
  last box is honestly open; `done` would be a false record. The refusal forced
  the event onto the board as `quiet-board-sweep`, which is better than a
  comment nobody schedules.
- **Do not write to the repo while a sweep runs.** The implementer did, and
  paid for it with a red it then had to disprove serially.
- **A `needs:` edge onto a `done` node is inert.** Offered as protection for
  the capsule keys; declined. `tests/capsule-recents.sh --keys` asserts the
  compiled key table verbatim and is the enforcement that actually holds.
- **"Silent" is not a state one session may act on across another's claim.**
