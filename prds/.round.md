# Round — six nodes landed; a measurement of mine was refuted and the correction is the better finding

## Established
- **`wezterm.config_builder()` is the whole difference.** It installs a
  validating `__newindex`; a plain `return { ... }` table installs none. At
  `ls-fonts --list-system` on `20240203`: clean 791 stdout / 0 stderr; + one
  bogus key 0 stdout / 350 stderr. **Both exit 0** — the exit code is never a
  predicate. `config_builder()` is what ships (`wezterm.lua:12`) · 11:58Z
- My 11:48Z measurement said this build *silently ignores* unknown keys and the
  check could never fail. **False** — it used a bare table. Skeptic refuted it;
  I reproduced the refutation myself · 11:58Z
- The gate's `named` harvest reads `config.<field>` out of the epic's **prose**,
  so quoting an invalid name as an example is read as a claim. It went red
  against the paragraph describing its own red. Fixed with a per-line
  `NOT-A-FIELD` marker; RED 1 and RED 2 still bite, and a marked line does not
  blind the next line · 12:45Z
- `agent-overview-derived-tools`' premise was **stale**: the idioms title has
  read `Search with rg, find with fd, pick with tv` since `0f9f635`
  (2026-08-24). Confirmed at `shell.nuon:394` · 13:00Z
- That shipped fix is **held by nothing**: revert the title in a staged corpus
  and `help-agent.sh`'s DISCOVERY check stays green — it reads the expected
  title out of the same corpus in the same run · analyst, 13:00Z
- d3 audit found **five** decision rows, not four — D.2 (odin-toolchain) in
  wave2.md was named by neither R1 nor R3 · 13:10Z
- S.10 census: class B = **0**, S.10 was the only node of its class; but
  `--validate` **cannot** detect a mispaired registry — its two assertions are
  independent set-coverage checks · analyst, 13:00Z
- Gates green now: `manual-coverage` 20 PASS/0 FAIL, `wave-status --validate`
  7 PASS EXIT=0, `wezterm-config-fields` rc 0 + selftest rc 0,
  `tree-links` 1661 links 0 broken · 13:15Z

## Decided
- **Call the skeptic before `done` on my own work.** It paid four times in one
  round: a false measurement, a false tick, a false sentence, two overclaimed
  epic closures.
- Epics get one real acceptance box + a verify that goes red, not a note
  arguing they need neither.
- 02-terminal box 1 ticks on the **user's new split-entry rating rule**, not on
  the reasoning being good. Amending the line to fit the result was rejected.
- The `needs:` cycle between agent-interface and derived-tools broken on
  derived-tools' side: the dep is on shipped code, not on a state that could
  not arrive until this node lands.

## Asked
- D.3 closures · **A** · landed at `5016922`
- Manual gates · user runs the **C.4 five** · script handed over · still out
- Rating-rules gap · **A**, split-entry rule · landed at `61dec66`
- derived-tools Q1 · **A**, keep the title, spend the node on the gate ·
  recorded, analyst re-dispatched on the narrowed contract

## Owed
- Collect the two live analysts (waves-s10 impl, derived-tools), then
  `doctor-debt-live-nodes` — which is orchestrator work, not a worker's.
