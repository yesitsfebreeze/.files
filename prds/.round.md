# Round — ten nodes landed; two of my own measurements were wrong and both are on the record

## Established
- **`config_builder()` is the whole difference** — validating `__newindex`; a
  bare `return { ... }` table has none. Clean 791/0, bogus key 0/350. **Both
  exit 0** · 11:58Z
- My 11:48Z claim that the build silently ignores unknown keys was **false**
  (bare-table probe). Skeptic refuted; I reproduced the refutation · 11:58Z
- The wezterm gate harvests `config.<field>` from **prose** → went red against
  its own documentation. Per-line `NOT-A-FIELD` marker; both REDs still
  bite · 12:45Z
- `agent-overview-derived-tools`' premise was **stale** (`shell.nuon:394` since
  `0f9f635`); the shipped fix was **held by nothing** — revert the title and
  `discovery_ok` stays green. Now gated: help-agent 90 → **100 PASS** · 14:20Z
- **The old tree-links fail-closed check could never go red**: the probe
  planted its marker at EOF, past every link, so an honoured marker exempted
  nothing. Fixed with a bait link. `--selftest` now rc 0 on derived tallies
  (`1668 + 13 = 1681`) · 14:40Z
- **`G.1` is `done` and `just gates` still exits 1** — 4813 PASS / 5 FAIL.
  `managed-config.sh`: undeclared `dot_local`, `litellm`. `shell-init.sh`: two
  apply reds, one asserting a **hand-written** television config survives an
  apply byte-identical · reproduced 14:55Z
- doctor: origin 45 → 44 after `from: tbd`; the 44 is exactly the counted
  `done`-node residue. The two numbers reconcile · 14:30Z
- Tripwire: requested-live 11 · derived-live 2. Far from tripping · 14:45Z

## Decided
- **Call the skeptic before `done` on my own work** — paid four times today.
- Boxes stay open rather than tick on a good argument: 02-terminal's rating box
  (until the user added the split-entry rule) and doctor-debt's box 1 (until
  the user chose `from: tbd`). Neither criterion was amended to fit a result.
- A stale contract is **rewritten with the old text kept** — derived-tools
  R1–R3.
- `0b77a71`'s two unmaintained gates share one cause → **memo**, since no PRD
  owns it: no spec means no `footprint:`, and the footprint is what tells an
  author which *shared* files a change touches.
- I set derived-tools `done` with its spec boxes unrun and reverted it to
  `claimed`. The spec's boxes are the evidence; the PRD's prose is not.

## Asked
- D.3 closures · **A** · `5016922`
- Rating-rules gap · **A**, split-entry rule · `61dec66`
- derived-tools Q1 · **A**, keep the title, gate it · `b3613ab`
- doctor-debt contradiction · **`from: tbd`** sentinel · `ac5103a`
- **Manual gates · the C.4 five AND now H.5 · still out, both need a person**

## Owed
- Collect the two live workers (tree-links impl, g1-gates analyst).
