diff --git a/AGENTS.md b/AGENTS.md
index 8f3aafa..5b1bb14 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -105,0 +106 @@ it is, not to decide what it says.
+| [`07-multiplexer`](prds/07-multiplexer/prd.md) | tmux — the portable layer: windows, panes, addressing, splits, copy, status, persistence (net-new) | 9 |
@@ -107,3 +108,6 @@ it is, not to decide what it says.
-85 nodes in all. Counts are direct children, with grandchildren in
-parentheses; `find prds -name prd.md` is the index, because node membership
-is by existence and a maintained list beside it goes stale.
+Counts are direct children, with grandchildren in parentheses.
+`find prds -name prd.md` is the index, because node membership is by existence
+and a maintained list beside it goes stale — **as the number that stood here
+had.** Corrected 2026-08-29: this paragraph opened "85 nodes in all" while
+`find` answered **173**, so the sentence warning that a maintained count goes
+stale was carrying one that had. No total is written here now; run the `find`.
diff --git a/gates/nushell-module-staging.sh b/gates/nushell-module-staging.sh
index 5bf74f9..bb22659 100755
--- a/gates/nushell-module-staging.sh
+++ b/gates/nushell-module-staging.sh
@@ -83 +83 @@ modules() {
-  $GREP -oE '^source ~/\.config/nushell/[a-z]+\.nu$' "$1" | sed 's|.*/||'
+  $GREP -oE '^source ~/\.config/nushell/[a-z-]+\.nu$' "$1" | sed 's|.*/||'
@@ -139 +139 @@ names_it() {
-       | $GREP -qE "(^|[^a-zA-Z0-9_])$stem(\.nu)?([^a-zA-Z0-9_.]|$)" \
+       | $GREP -qE "(^|[^a-zA-Z0-9_-])$stem(\.nu)?([^a-zA-Z0-9_.-]|$)" \
@@ -346 +346 @@ selftest() {
-    "$TV" 's/^([[:space:]]*for m in [a-z ]*copymode) help(; do)/\1\2/'
+    "$TV" 's/^([[:space:]]*for m in [a-z -]*help-check) help(; do)/\1\2/'
@@ -352 +352 @@ selftest() {
-    "$TV" 's/^([[:space:]]*for m in [a-z ]*copymode)(; do)/\1 help\2/'
+    "$TV" 's/^([[:space:]]*for m in [a-z -]*help-check)(; do)/\1 help\2/'
diff --git a/home/dot_config/nushell/config.nu b/home/dot_config/nushell/config.nu
index 1bbaa58..78f25de 100644
--- a/home/dot_config/nushell/config.nu
+++ b/home/dot_config/nushell/config.nu
@@ -608,0 +609 @@ alias core-help = help
+source ~/.config/nushell/help-check.nu
diff --git a/home/dot_config/nushell/help.nu b/home/dot_config/nushell/help.nu
index 5770349..a8fb6c6 100644
--- a/home/dot_config/nushell/help.nu
+++ b/home/dot_config/nushell/help.nu
@@ -669,0 +670 @@ def help [
+    --check                # diff the manual against the live configuration
@@ -705,0 +707,8 @@ def help [
+    # 0 — the drift check (06-help/04-drift-check). FIRST, and it delegates
+    # to help-check.nu rather than doing the work here: this file carries R8's
+    # no-spawn guarantee and the check spawns `nvim --headless`, so every line
+    # that spawns lives in the other file and only the flag lives here.
+    # config.nu sources help-check.nu ABOVE this file, because a def calling a
+    # def from a LATER source fails at run time with `Command not found`.
+    if $check { return (_help_check) }
+
diff --git a/home/dot_config/nushell/help/nvim.nuon b/home/dot_config/nushell/help/nvim.nuon
index 93adad2..3b86e12 100644
--- a/home/dot_config/nushell/help/nvim.nuon
+++ b/home/dot_config/nushell/help/nvim.nuon
@@ -380,0 +381,15 @@
+    {
+        key: "<leader>ss <leader>sl <leader>sd"
+        title: "Restore the buffers you had open here"
+        use: "The session for a directory is written automatically when you quit Neovim with at least one real file open. `<leader>ss` restores the one belonging to the directory you are in, `<leader>sl` restores the most recent session from anywhere, and `<leader>sd` turns the save off for this run when you do not want what you are about to do remembered. Nothing is restored unless you ask: starting `nvim` plainly gives you an empty editor, as it always did."
+        topic: "edit"
+        mode: "nvim:normal"
+        also: ["<leader>w and <leader>q"]
+        why: "This is what tmux-resurrect brings back after a reboot: it re-launches nvim in the pane's directory with the restore command, and without a written session that pane would come back as an empty editor in the right place. The keys sit on `<leader>s` and not persistence.nvim's documented `<leader>q` because `<leader>q` is already Quit here, and making it a prefix would leave every quit waiting `timeoutlen` for a second key."
+        verify: [
+            {kind: "nvim-map", mode: "n", lhs: "<leader>ss", desc: "Restore session for this directory"}
+            {kind: "nvim-map", mode: "n", lhs: "<leader>sl", desc: "Restore last session"}
+            {kind: "nvim-map", mode: "n", lhs: "<leader>sd", desc: "Stop session save on exit"}
+        ]
+        source: "prds/07-multiplexer/06-nvim-session/prd.md"
+    }
diff --git a/home/dot_config/nushell/help/use-review.nuon b/home/dot_config/nushell/help/use-review.nuon
index a9270a4..08d0143 100644
--- a/home/dot_config/nushell/help/use-review.nuon
+++ b/home/dot_config/nushell/help/use-review.nuon
@@ -193,0 +194 @@
+    {id: "<leader>ss <leader>sl <leader>sd", file: "nvim.nuon", digest: "b3919ace8b0a107e", reviewer: "reader-07-06-nvim-session", author: "analyst-07-06-nvim-session", date: "2026-08-29", note: "independent reading by a session that wrote none of this text, against the plugin source rather than the spec. I read persistence/init.lua and config.lua. VimLeavePre in M.start saves only when at least need (default 1) named, ordinary-buftype buffers are open, matching the use's at least one real file open claim. leader-ss calls load() which resolves the cwd plus branch session, falling back to the non-branch file for that directory, matching restores the one belonging to the directory you are in. leader-sl calls load with last true, which is list()[1], sorted by mtime across all session files, matching most recent from anywhere. leader-sd calls stop, which deletes the augroup so nothing saves this run. setup only calls start, never an auto-load, so a plain nvim opens empty. No overclaim found. UNVERIFIED AND NOT TICKED: the three nvim-map verify targets cannot be resolved today, because the drift check that reads them (06-help/04-drift-check) is open and parse-erroring; the entry is documented and machine-unverified until it lands."}
diff --git a/home/dot_config/nushell/help/why-review.nuon b/home/dot_config/nushell/help/why-review.nuon
index 10ddaca..fe499b7 100644
--- a/home/dot_config/nushell/help/why-review.nuon
+++ b/home/dot_config/nushell/help/why-review.nuon
@@ -125,0 +126 @@
+    {id: "<leader>ss <leader>sl <leader>sd", file: "nvim.nuon", digest: "a3d233b599fb26ea", reviewer: "reader-07-06-nvim-session", author: "analyst-07-06-nvim-session", date: "2026-08-29", note: "independent reading by a session that wrote none of this text. keymaps.lua line 47 confirms leader-q is a plain leaf map, cmd quit, desc Quit, so the timeoutlen-prefix reasoning holds. tmux-resurrect's nvim_session.sh does take the pane DIRECTORY and echo a restore command (nvim -S when a Session.vim exists, else the original command), so re-launches nvim in the pane's directory is accurate as a general description of the strategy family, though this literal script is the Session.vim strategy rather than the custom-command inline strategy session.lua's own comments say this config actually uses. The why adds new information not in use: the reboot/tmux-resurrect motivation and the leader-q-collision rationale for the keybinding choice, no restatement. Word count: 69."}
diff --git a/home/dot_config/nvim/lazy-lock.json b/home/dot_config/nvim/lazy-lock.json
index 253b58d..44ceefe 100644
--- a/home/dot_config/nvim/lazy-lock.json
+++ b/home/dot_config/nvim/lazy-lock.json
@@ -14,0 +15 @@
+  "persistence.nvim": { "branch": "main", "commit": "b20b2a7887bd39c1a356980b45e03250f3dce49c" },
diff --git a/home/dot_config/nvim/lua/plugins/which-key.lua b/home/dot_config/nvim/lua/plugins/which-key.lua
index d041fb3..0adf10d 100644
--- a/home/dot_config/nvim/lua/plugins/which-key.lua
+++ b/home/dot_config/nvim/lua/plugins/which-key.lua
@@ -51,0 +52,6 @@ return {
+      -- Added by 07-multiplexer/06-nvim-session. It goes AFTER table, not in
+      -- alphabetical order: tests/nvim-small-plugins.sh asserts R2's five
+      -- groups as five ASCENDING line numbers, so inserting anywhere among
+      -- them would go red on the order check while the set check still
+      -- passed. Appending leaves f, b, c, r, t in the order R2 states.
+      { "<leader>s", group = "session" },
diff --git a/prds/.round.md b/prds/.round.md
index 719d529..8c995d6 100644
--- a/prds/.round.md
+++ b/prds/.round.md
@@ -1 +1 @@
-# Round — four of five worked. Two nodes closed, one built, two open.
+# Round — three workers out, two verdicts landed, the board grew by eight nodes.
@@ -4,13 +4,12 @@
-1. **`00-delivery/corrections/g1-verify-still-red-on-just-gates`** — `specced`,
-   w85, 24 boxes, untouched this round. Still the largest thing ready.
-2. **`05-platform/01-deploy-mechanism`** — R7 is BUILT and both platform gates
-   are green, but the seeding path has **never run against a cold
-   `<data>/mason`**. Five boxes open; the fresh-machine ones need a machine.
-3. **`05-platform/02-package-provisioning`** — three open. The idempotence line
-   ("a second run installs nothing") needs a scenario nobody has written.
-4. **`00-delivery/corrections/questions-check-red-only-on-history`** — R1 was
-   REVERSED this round (see below) and R2 lands in `~/dev/infra/pearde`, a
-   different repo. Nothing implemented.
-5. **`01-capsule`** the epic — now unblocked, three real acceptance boxes and
-   `verify: ""`. The third ("`git push` and SSH inside the container without
-   re-authentication") needs a real remote. **Do not close it on an argument.**
+1. **`07-multiplexer/01-session-and-windows`** — `claimed`, implementer, 34
+   boxes. spec01 (`tmux.conf`) and spec02 (`tmux-main`) are BUILT and measured
+   on this host by the analyst; **spec03 (`tests/tmux-session-and-windows.sh`)
+   is unwritten** and is the bulk of the work.
+2. **`06-help/04-drift-check/01-check-plumbing`** — `analyzing`. Child 01 of 7;
+   four siblings gate on it. Its whole point is the gate that could not fail
+   (see Established).
+3. **`07-multiplexer/06-nvim-session`** — `analyzing` since 09:18. Dropped by
+   machine sleep six times; slowest of the three.
+4. **`06-help/04-drift-check` children 02-07** — all `open`, gated on 01.
+5. **Not mine this round.** `00-delivery`, `05-platform`, `01-capsule` belong to
+   `dotfiles-9e` by agreement — see Decided.
@@ -21,9 +19,0 @@ Nothing is out with the user.
-**A second session is on this board.** `dotfiles-ef` added the ten-node
-`07-multiplexer` epic and the memo
-`tmux-owns-multiplexing-wezterm-keeps-the-chrome`, which **withdraws
-`02-terminal` I1 and reverses half of I2** — tmux takes windows, panes and
-scrollback; WezTerm keeps font, centering, opacity and the capsule keys. It is
-also editing `AGENTS.md` and `prds/README.md`. Every commit this round was made
-with explicit paths and touched none of its files. Two orchestrators on one
-board is the thing the protocol says not to do; that is the user's to settle.
-
@@ -31,15 +21,25 @@ board is the thing the protocol says not to do; that is the user's to settle.
-- **The C.4 five run.** `bash tests/capsule-recents-gui.sh` → **35 run, 35
-  passed, EXIT=0**, `--selftest` → **4 run, 4 passed**. Node `done` at
-  `6151ae5`.
-- **H.5 ticked on the filed evidence**; `06-help/05-agent-interface` `done` at
-  `4475488`. `06-help/04-drift-check` is dispatchable for the first time.
-- **`grep -c` exits 1 when the count is zero**, and two spec verify blocks
-  ended on one. The runner uses `set -e -o pipefail` — reproduced: the same
-  block is rc 0 under plain bash and rc 1 under `bash -e -o pipefail`, which is
-  exactly what `collect` reported. Both blocks now print the number AND assert
-  it.
-- **`plan.py` never reads `mode:`.** Only `questions.py` does, at
-  `WAITING = (…"hitl"…)` :73 and :206. So `hitl` on a `done` node is a true
-  record of the work, not a queue state.
-- The questions-check shape split is **8 / 8 / 2 / 9 / 2**, counted by
-  predicate. A first pass guessed 9/6/14 and was wrong on all three.
+- **The machine is sleeping mid-response and killing agents.** Error text names
+  it: "Your computer went to sleep mid-response". Six-plus drops across four
+  workers. `caffeinate -i -t 3600` started 10:2x. Progress survives each drop;
+  resume, never respawn.
+- **`help --check`'s parse error was a missing flag, not a broken file.**
+  `nu -c 'source help.nu'` exits 0; `help --check` gives
+  `nu::parser::unknown_flag`. `def help` had nine flags and no `--check`.
+- **`gates/nushell-module-staging.sh` cannot fail on a hyphenated module.** Its
+  list regex is `^source ~/\.config/nushell/[a-z]+\.nu$`. `help-check.nu` never
+  enters the list, the gate prints "misses: 0", and eleven sibling gates are
+  dead at parse (`nu::parser::sourced_file_not_found`, proven directly). Fix is
+  `[a-z-]+` plus the eleven `MODULES=` constants — folded into `01-check-plumbing`.
+- **`nvim --headless` with no nvim config silently returns Neovim's own 123
+  defaults instead of 217**, and reported 54 stale + 1 mismatched, every one
+  false. `--clean` does the same by a second route. Caught only because a
+  standalone probe had measured 214 first.
+- **F14 corrects F3 on tmux cwd.** `new-window`'s cwd is the *pane's* when typed
+  in a shell, the *client's* from outside tmux, the *server's* under
+  `run-shell`, the *session's* from a key binding — only the last is pressed.
+  A fixture with client cwd and session path in one directory cannot tell them
+  apart. Any box saying "a bare `new-window` lands at `~`" is false three ways
+  out of four; testing key dispatch needs a nested tmux.
+- **I5's four `desc` failures do not exist.** Measured: absent 0, null 7,
+  explicit 59, zero mismatches. I5 amended; memo written.
+- `chezmoi source-path` = `/Users/feb/dev/.files/home`, run not assumed.
@@ -48,15 +48,27 @@ board is the thing the protocol says not to do; that is the user's to settle.
-- **Six runs of one harness, and every red but one was a check that could not
-  discriminate** — not a defect in the thing under test. The rule is
-  [`an-absence-assertion-needs-a-positive-precondition`](memos/an-absence-assertion-needs-a-positive-precondition-or-it-is-green-on-a-blank-screen.md):
-  never assert an absence without asserting the screen exists.
-- **R1 of the doctor node was reversed rather than implemented.** Its premise
-  was false, and sweeping the eight would have deleted a true record to green a
-  check — option 2's failure mode, reached through the half that looked
-  mechanical.
-- **A requirement that contradicts an invariant gets neither obeyed nor
-  ignored.** R7 named `install.sh`; §4 IS `chezmoi apply`, which a gate pins as
-  last. I1 — "apply-time, not launch-time" — is what a `run_after` satisfies.
-- **The frontmost guard earned itself before the first check ran.** With a
-  shared bundle id macOS activates by bundle: `set frontmost` on the probe
-  reported success and left the USER's window frontmost. Unguarded, those
-  keystrokes were going into a live session.
+- **The board is divided with `dotfiles-9e`, by agreement.** I hold
+  `07-multiplexer` and `06-help`; it holds `00-delivery`, `05-platform`,
+  `01-capsule`. Its footprint is `tests/shell-init.sh`, `tests/managed-config.sh`,
+  `tests/shell-litellm.sh`, `home/dot_config/litellm/`. `dotfiles-ef`, the
+  session the last round flagged as a collision, is gone.
+- **A census inside an invariant is a measurement wearing an invariant's
+  clothes.** Memo
+  [`an-invariant-naming-a-defect-must-be-re-measured-before-a-child-inherits-it`](memos/an-invariant-naming-a-defect-must-be-re-measured-before-a-child-inherits-it.md).
+  A child re-measures before inheriting; the invariant is amended to what was
+  found, or the finding says it never held.
+- **The two capsule keys get a named constraint, not a `needs:` edge.**
+  `Ctrl+Shift+S`/`Ctrl+Shift+O` survive `08-wezterm-reduction`; a `needs:` on
+  `01-capsule/04-recent-workspaces` would gate nothing because that node is
+  already `done`. Constraint written into `08-wezterm-reduction`'s body with
+  the four things that rest on those keys.
+- **`04-drift-check` REFINEd rather than specced**, on seven units summing ~88
+  across three distinct contracts — a resolver, a ~281-handle classification,
+  and a `tmux-key` kind against a `tmux.conf` being built concurrently.
+
+## Findings handed to `dotfiles-9e` (in its half, not mine)
+- **tmux is in no host package list.** `install.sh`'s `PKGS` has no `tmux=tmux`;
+  tmux is in the capsule `Dockerfile` only. The epic calls it a hard dependency.
+  `05-platform/02-package-provisioning`.
+- **`gates/waves.tsv` cannot register a `07-multiplexer` gate** — rows key on
+  `task:` ids and no node in that epic carries one. `00-delivery`.
+- **`prds/02-terminal/04-copy-mode/prd.md` is `state: done` with every
+  requirement box `- [ ]`.** Ownerless; corrections backlog.
diff --git a/prds/.transitions.jsonl b/prds/.transitions.jsonl
index 48c8369..1762380 100644
--- a/prds/.transitions.jsonl
+++ b/prds/.transitions.jsonl
@@ -4,0 +5,20 @@
+{"calls": null, "from": "blocked", "prd": "01-capsule/04-recent-workspaces", "reads": null, "refused": null, "t": "2026-08-29T08:50:43", "to": "specced", "tokens": null}
+{"calls": null, "from": "specced", "prd": "01-capsule/04-recent-workspaces", "reads": null, "refused": null, "t": "2026-08-29T08:50:48", "to": "claimed", "tokens": null}
+{"calls": null, "from": null, "prd": "07-multiplexer", "reads": null, "refused": null, "t": "2026-08-29T08:50:56", "to": "open", "tokens": null}
+{"from": "claimed", "prd": "01-capsule/04-recent-workspaces", "t": "2026-08-29 08:56", "to": "done"}
+{"calls": null, "from": "open", "prd": "07-multiplexer/01-session-and-windows", "reads": null, "refused": null, "t": "2026-08-29T09:18:11", "to": "analyzing", "tokens": null}
+{"calls": null, "from": "open", "prd": "07-multiplexer/06-nvim-session", "reads": null, "refused": null, "t": "2026-08-29T09:18:12", "to": "analyzing", "tokens": null}
+{"calls": null, "from": "open", "prd": "06-help/04-drift-check", "reads": null, "refused": null, "t": "2026-08-29T09:18:12", "to": "analyzing", "tokens": null}
+{"calls": null, "from": "specced", "prd": "00-delivery/corrections/g1-verify-still-red-on-just-gates", "reads": null, "refused": null, "t": "2026-08-29T10:35:16", "to": "claimed", "tokens": null}
+{"calls": null, "from": "analyzing", "prd": "06-help/04-drift-check", "reads": null, "refused": null, "t": "2026-08-29T10:37:50", "to": "open", "tokens": null}
+{"calls": null, "from": "analyzing", "prd": "07-multiplexer/01-session-and-windows", "reads": null, "refused": null, "t": "2026-08-29T10:39:12", "to": "specced", "tokens": null}
+{"calls": null, "from": "specced", "prd": "07-multiplexer/01-session-and-windows", "reads": null, "refused": null, "t": "2026-08-29T10:39:15", "to": "claimed", "tokens": null}
+{"calls": null, "from": "open", "prd": "06-help/04-drift-check/01-check-plumbing", "reads": null, "refused": null, "t": "2026-08-29T10:40:03", "to": "analyzing", "tokens": null}
+{"calls": null, "from": null, "prd": "00-delivery/corrections/02-terminal-closed-six-children-with-41-requirement-boxes-ne", "reads": null, "refused": null, "t": "2026-08-29T10:47:22", "to": "open", "tokens": null}
+{"calls": null, "from": null, "prd": "05-platform/02-package-provisioning/tmux-is-a-host-dependency-of-07-multiplexer-and-is-in-no-hos", "reads": null, "refused": null, "t": "2026-08-29T10:47:56", "to": "open", "tokens": null}
+{"calls": null, "from": null, "prd": "00-delivery/gates-waves-tsv-cannot-register-a-gate-for-any-node-without-", "reads": null, "refused": null, "t": "2026-08-29T10:48:21", "to": "open", "tokens": null}
+{"calls": null, "from": "analyzing", "prd": "07-multiplexer/06-nvim-session", "reads": null, "refused": null, "t": "2026-08-29T10:50:07", "to": "specced", "tokens": null}
+{"calls": null, "from": "specced", "prd": "07-multiplexer/06-nvim-session", "reads": null, "refused": null, "t": "2026-08-29T10:50:17", "to": "claimed", "tokens": null}
+{"calls": null, "from": null, "prd": "00-delivery/a-green-just-gates-needs-one-serial-sweep-on-a-quiet-board", "reads": null, "refused": null, "t": "2026-08-29T11:42:59", "to": "open", "tokens": null}
+{"calls": null, "from": "claimed", "prd": "00-delivery/corrections/g1-verify-still-red-on-just-gates", "reads": null, "refused": null, "t": "2026-08-29T11:44:00", "to": "blocked", "tokens": null}
+{"calls": null, "from": "open", "prd": "00-delivery/corrections/done-nodes-with-unticked-boxes", "reads": null, "refused": null, "t": "2026-08-29T12:17:19", "to": "analyzing", "tokens": null}
diff --git a/prds/00-delivery/corrections/done-nodes-with-unticked-boxes/prd.md b/prds/00-delivery/corrections/done-nodes-with-unticked-boxes/prd.md
index cd8d904..3f728e3 100644
--- a/prds/00-delivery/corrections/done-nodes-with-unticked-boxes/prd.md
+++ b/prds/00-delivery/corrections/done-nodes-with-unticked-boxes/prd.md
@@ -2 +2 @@
-state: open
+state: analyzing
@@ -10 +10 @@ from: 07-multiplexer/01-session-and-windows
-claim:
+claim: analyst 2026-08-29 12:17
diff --git a/prds/06-help/04-drift-check/prd.md b/prds/06-help/04-drift-check/prd.md
index fe1961c..3f77510 100644
--- a/prds/06-help/04-drift-check/prd.md
+++ b/prds/06-help/04-drift-check/prd.md
@@ -160,0 +161,12 @@ terminal surface, and R5 closes for shell and Neovim only.
+
+## Children
+
+| child | contract | needs |
+|---|---|---|
+| `01-check-plumbing` | help --check` parses and runs: the flag on `def help`, `help-check.nu`, config.nu source order, and the eleven sibling `MODULES=` constants plus the `[a-z-]+` regex fix so no gate dies at parse | — |
+| `02-shell-resolver` | R1 resolves keybinding/alias/command in a configured shell, and the four wrong-kind corpus defects are reported | 01-check-plumbing |
+| `03-nvim-resolver` | R2 for global maps: one headless spawn that never installs plugins and raises on a missing config, the seven normalization rules, the three-state `desc | 01-check-plumbing |
+| `04-nvim-buffer-maps` | The eight `scope: "buffer"` targets resolve against an LSP-attached and filetype-loaded buffer, or the check declares them unresolvable with the measurement | 03-nvim-resolver |
+| `05-allowlist` | R6: the ~281 live handles classified explicitly, the `_`-private convention written down, the LSP-defaults exception kept documented | 02-shell-resolver, 03-nvim-resolver |
+| `06-report-and-gate` | R4/R5/R7 report and exit code, and `tests/help-drift-check.sh` proving all four acceptance mutations including the Neovim-default collision | 05-allowlist, 04-nvim-buffer-maps |
+| `07-tmux-key-resolver` | The memo's `tmux-key` verify kind resolves against `tmux -L … list-keys`, and terminal.nuon's ~20 `wezterm-key` entries convert | 01-check-plumbing |
diff --git a/prds/06-help/prd.md b/prds/06-help/prd.md
index 0d708ce..fa3a5a4 100644
--- a/prds/06-help/prd.md
+++ b/prds/06-help/prd.md
@@ -60 +60 @@ targets — enforced in `tests/help-content-model.nu`, whose
-  | `desc: null` | the live map carries **no** description on purpose — assert existence only, never a mismatch (14 targets do this today) |
+  | `desc: null` | the live map carries **no** description on purpose — assert existence only, never a mismatch (**7** targets do this, measured 2026-08-29; this cell read 14 until then) |
@@ -72 +72,2 @@ targets — enforced in `tests/help-content-model.nu`, whose
-    the field. It bites today: four `nvim-map` targets recorded in
+    the field. **The four failures this bullet promised do not exist.**
+    Corrected 2026-08-29: it named four `nvim-map` targets from
@@ -74,3 +75,9 @@ targets — enforced in `tests/help-content-model.nu`, whose
-    omit `desc` where the live maps set `LSP: …` strings — four drift-check
-    false failures waiting. They belong to the content files and to
-    [04-drift-check](04-drift-check/prd.md), not to this epic.
+    that omit `desc` where the live maps set `LSP: …` strings, and called
+    them "four drift-check false failures waiting". The `04-drift-check`
+    analyst built the check and measured the live corpus: `desc` **absent 0**,
+    **null 7**, **explicit 59**, and **zero** desc mismatches. The record does
+    not say whether the corpus was fixed after this was written or the count
+    was never right. The clause above still binds — an omitted `desc` on a map
+    that carries one live is still a defect — but no child inherits an
+    exception for four targets that are not there. See
+    [`an-invariant-naming-a-defect-must-be-re-measured`](../memos/an-invariant-naming-a-defect-must-be-re-measured-before-a-child-inherits-it.md).
diff --git a/prds/07-multiplexer/01-session-and-windows/prd.md b/prds/07-multiplexer/01-session-and-windows/prd.md
index 9c52efb..9551b2c 100644
--- a/prds/07-multiplexer/01-session-and-windows/prd.md
+++ b/prds/07-multiplexer/01-session-and-windows/prd.md
@@ -2 +2 @@
-state: open        # open|analyzing|refine|question|specced|claimed|blocked|done|failed
+state: claimed        # open|analyzing|refine|question|specced|claimed|blocked|done|failed
@@ -6,2 +6,2 @@ priority: 20        # higher first
-complexity: 0      # analyst, at spec time — 1-100. THE WEIGHT the board schedules by
-blast-radius:      # analyst, at spec time — high|mid|low. What breaks if this is wrong
+complexity: 32      # analyst, at spec time — 1-100. THE WEIGHT the board schedules by
+blast-radius: high
@@ -15,0 +16 @@ time:              # OPTIONAL. See @references/parts/order.md
+claim: implementer 2026-08-29 10:39
diff --git a/prds/07-multiplexer/06-nvim-session/prd.md b/prds/07-multiplexer/06-nvim-session/prd.md
index 6c74979..fe29991 100644
--- a/prds/07-multiplexer/06-nvim-session/prd.md
+++ b/prds/07-multiplexer/06-nvim-session/prd.md
@@ -2 +2 @@
-state: open        # open|analyzing|refine|question|specced|claimed|blocked|done|failed
+state: claimed        # open|analyzing|refine|question|specced|claimed|blocked|done|failed
@@ -6,2 +6,2 @@ priority: 20        # higher first
-complexity: 0      # analyst, at spec time — 1-100. THE WEIGHT the board schedules by
-blast-radius:      # analyst, at spec time — high|mid|low. What breaks if this is wrong
+complexity: 27      # analyst, at spec time — 1-100. THE WEIGHT the board schedules by
+blast-radius: mid
@@ -15,0 +16 @@ time:              # OPTIONAL. See @references/parts/order.md
+claim: implementer 2026-08-29 10:50
diff --git a/prds/07-multiplexer/08-wezterm-reduction/prd.md b/prds/07-multiplexer/08-wezterm-reduction/prd.md
index 51a0593..b5abd94 100644
--- a/prds/07-multiplexer/08-wezterm-reduction/prd.md
+++ b/prds/07-multiplexer/08-wezterm-reduction/prd.md
@@ -44,0 +45,24 @@ The cutover, in one change (Q8): `default_prog` attaches tmux, `enable_tab_bar =
+## Constraint — the two capsule keys are named, not implied
+
+`Ctrl+Shift+S` and `Ctrl+Shift+O` survive this reduction. They are the capsule
+recents picker, and the prose clause "the capsule `SendString` keys" above is
+not enough to hold them, because what rests on those two keys is specific and
+already `done`:
+
+- [`01-capsule/04-recent-workspaces`](../../01-capsule/04-recent-workspaces/prd.md)
+  R2 and R3, and three of its acceptance boxes.
+- The five C.4 rows in `gates/manual/wave4.md`, ticked on real hardware on
+  2026-08-29.
+- `tests/capsule-recents-gui.sh`, which drives both keys through a real GUI
+  WezTerm (35 run, 35 passed).
+- `tests/capsule-recents.sh --keys`, which asserts the **compiled** WezTerm key
+  table verbatim and goes red the moment either key moves.
+
+So the implementer of this node deletes F5, F6 and the tab machinery and leaves
+those two bindings exactly where they are, and `tests/capsule-recents.sh --keys`
+staying green is a check on this node, not a coincidence. Recorded 2026-08-29
+after the session holding `01-capsule` raised it: the dependency is real and is
+deliberately **not** in this node's `needs:` — `04-recent-workspaces` is already
+`done`, so a `needs:` edge would gate nothing. A named constraint is what a
+finished-but-load-bearing sibling gets instead.
+
diff --git a/tests/help-agent.sh b/tests/help-agent.sh
index 876f20d..3cea82f 100644
--- a/tests/help-agent.sh
+++ b/tests/help-agent.sh
@@ -147 +147 @@ HOST_ONLY=' (host-only — the terminal is outside a capsule)'
-MODULES="dirstack.nu pass.nu claude.nu litellm.nu recents.nu zoxide.nu history.nu capsule.nu finder.nu quicklist.nu copymode.nu help.nu theme.nu"
+MODULES="dirstack.nu pass.nu claude.nu litellm.nu recents.nu zoxide.nu history.nu capsule.nu finder.nu quicklist.nu copymode.nu help-check.nu help.nu theme.nu"
@@ -151 +151 @@ modules_fresh() {
-  got="$($GREP -oE '^source ~/\.config/nushell/[a-z]+\.nu$' "$CONFIG_NU" \
+  got="$($GREP -oE '^source ~/\.config/nushell/[a-z-]+\.nu$' "$CONFIG_NU" \
diff --git a/tests/help-browser.sh b/tests/help-browser.sh
index 5a95b94..0033c02 100644
--- a/tests/help-browser.sh
+++ b/tests/help-browser.sh
@@ -517 +517 @@ mk_machine() {
-  for m in dirstack pass theme claude litellm recents zoxide history capsule finder quicklist copymode help; do
+  for m in dirstack pass theme claude litellm recents zoxide history capsule finder quicklist copymode help-check help; do
diff --git a/tests/nushell-aliases.sh b/tests/nushell-aliases.sh
index d56f20a..228d66d 100644
--- a/tests/nushell-aliases.sh
+++ b/tests/nushell-aliases.sh
@@ -130,0 +131 @@ cp "$NUSHELL_SRC/finder.nu" "$M/home/.config/nushell/finder.nu"  # 04-shell/04:
+  cp "$NUSHELL_SRC/help-check.nu" "$M/home/.config/nushell/help-check.nu"  # 06-help/04: config.nu sources help-check.nu ABOVE help.nu
diff --git a/tests/nushell-core.sh b/tests/nushell-core.sh
index 3e5f811..a7b03df 100755
--- a/tests/nushell-core.sh
+++ b/tests/nushell-core.sh
@@ -398,0 +399 @@ cp "$NUSHELL_SRC/finder.nu" "$M/home/.config/nushell/finder.nu"  # 04-shell/04:
+  cp "$NUSHELL_SRC/help-check.nu" "$M/home/.config/nushell/help-check.nu"  # 06-help/04: config.nu sources help-check.nu ABOVE help.nu
diff --git a/tests/nvim-options.sh b/tests/nvim-options.sh
index 7ec138c..61f5bd4 100644
--- a/tests/nvim-options.sh
+++ b/tests/nvim-options.sh
@@ -231,2 +231,2 @@ stage_tree() {
-  [ "$files" = "./init.lua ./lazy-lock.json ./lua/config/autocmds.lua ./lua/config/keymaps.lua ./lua/config/lazy.lua ./lua/config/options.lua ./lua/config/shift-select.lua ./lua/plugins/autopairs.lua ./lua/plugins/colorscheme.lua ./lua/plugins/completion.lua ./lua/plugins/conform.lua ./lua/plugins/explorer.lua ./lua/plugins/gitsigns.lua ./lua/plugins/init.lua ./lua/plugins/lsp.lua ./lua/plugins/statusline.lua ./lua/plugins/table-mode.lua ./lua/plugins/telescope.lua ./lua/plugins/treesitter.lua ./lua/plugins/which-key.lua" ]
-  chk "tree: home/dot_config/nvim/ holds exactly the post-E.14 census — gitsigns.lua, which-key.lua, autopairs.lua and E.14's lua/config/shift-select.lua included (got: $files)" $?
+  [ "$files" = "./init.lua ./lazy-lock.json ./lua/config/autocmds.lua ./lua/config/keymaps.lua ./lua/config/lazy.lua ./lua/config/options.lua ./lua/config/shift-select.lua ./lua/plugins/autopairs.lua ./lua/plugins/colorscheme.lua ./lua/plugins/completion.lua ./lua/plugins/conform.lua ./lua/plugins/explorer.lua ./lua/plugins/gitsigns.lua ./lua/plugins/init.lua ./lua/plugins/lsp.lua ./lua/plugins/session.lua ./lua/plugins/statusline.lua ./lua/plugins/table-mode.lua ./lua/plugins/telescope.lua ./lua/plugins/treesitter.lua ./lua/plugins/which-key.lua" ]
+  chk "tree: home/dot_config/nvim/ holds exactly the census — the post-E.14 set plus lua/plugins/session.lua, added by 07-multiplexer/06-nvim-session (got: $files)" $?
diff --git a/tests/shell-claude.sh b/tests/shell-claude.sh
index dd1c2d8..291695c 100644
--- a/tests/shell-claude.sh
+++ b/tests/shell-claude.sh
@@ -339,0 +340 @@ cp "$NUSHELL_SRC/finder.nu" "$M/home/.config/nushell/finder.nu"  # 04-shell/04:
+  cp "$NUSHELL_SRC/help-check.nu" "$M/home/.config/nushell/help-check.nu"  # 06-help/04: config.nu sources help-check.nu ABOVE help.nu
diff --git a/tests/shell-help.sh b/tests/shell-help.sh
index 55986aa..c467acc 100644
--- a/tests/shell-help.sh
+++ b/tests/shell-help.sh
@@ -101 +101 @@ PRD_PATH="prds/06-help/02-help-command/prd.md"
-MODULES="pass.nu claude.nu litellm.nu recents.nu zoxide.nu history.nu capsule.nu finder.nu quicklist.nu copymode.nu help.nu"
+MODULES="pass.nu claude.nu litellm.nu recents.nu zoxide.nu history.nu capsule.nu finder.nu quicklist.nu copymode.nu help-check.nu help.nu"
diff --git a/tests/shell-history.sh b/tests/shell-history.sh
index 8702c39..086378a 100644
--- a/tests/shell-history.sh
+++ b/tests/shell-history.sh
@@ -545,0 +546 @@ mk_machine() {
+  cp "$NUSHELL_SRC/help-check.nu" "$M/home/.config/nushell/help-check.nu"  # 06-help/04: config.nu sources help-check.nu ABOVE help.nu
diff --git a/tests/shell-listing.sh b/tests/shell-listing.sh
index 5e5e363..38937da 100644
--- a/tests/shell-listing.sh
+++ b/tests/shell-listing.sh
@@ -444,0 +445 @@ cp "$NUSHELL_SRC/finder.nu" "$M/home/.config/nushell/finder.nu"  # 04-shell/04:
+  cp "$NUSHELL_SRC/help-check.nu" "$M/home/.config/nushell/help-check.nu"  # 06-help/04: config.nu sources help-check.nu ABOVE help.nu
diff --git a/tests/shell-quicklist.sh b/tests/shell-quicklist.sh
index ec148ec..6e6b8b5 100644
--- a/tests/shell-quicklist.sh
+++ b/tests/shell-quicklist.sh
@@ -671 +671 @@ mk_machine() {
-  for m in dirstack pass theme claude litellm recents zoxide history capsule finder quicklist copymode help; do
+  for m in dirstack pass theme claude litellm recents zoxide history capsule finder quicklist copymode help-check help; do
diff --git a/tests/shell-television.sh b/tests/shell-television.sh
index 2f1858b..d5e06f9 100644
--- a/tests/shell-television.sh
+++ b/tests/shell-television.sh
@@ -565 +565 @@ mk_machine() {
-  for m in dirstack pass theme claude litellm recents zoxide history capsule finder quicklist copymode help; do
+  for m in dirstack pass theme claude litellm recents zoxide history capsule finder quicklist copymode help-check help; do
diff --git a/tests/shell-zoxide.sh b/tests/shell-zoxide.sh
index 8e73b82..59c255f 100644
--- a/tests/shell-zoxide.sh
+++ b/tests/shell-zoxide.sh
@@ -599,0 +600 @@ cp "$NUSHELL_SRC/finder.nu" "$M/home/.config/nushell/finder.nu"  # 04-shell/04:
+  cp "$NUSHELL_SRC/help-check.nu" "$M/home/.config/nushell/help-check.nu"  # 06-help/04: config.nu sources help-check.nu ABOVE help.nu
