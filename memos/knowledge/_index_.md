<!-- generated by scripts/memos-check.py; edit the memos, not this file -->

# knowledge

| memo | is | read when |
|---|---|---|
| [[every-abort-measurement-was-missing-wget]] | every measurement of the offline first-save abort ran on a PATH with no wget, which the machine has — the bug is unmeasured on a faithful PATH | measuring an offline behaviour, or trusting the mason abort numbers |
| [[llm-launches-any-agent]] | `llm -a <program>` starts any agent on the proxy — the program is never registered, only its default args and env are | launching an agent through the proxy, or adding one to agents.json |
| [[mason-refresh-off-trades-auto-bootstrap]] | mason's registry refresh is off — launching Neovim offline discarded the first save; the fresh-machine bootstrap moves to install.sh | touching nvim's lsp.lua, mason, or the fresh-machine install path |
