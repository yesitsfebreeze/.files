---
kind: type
type: question
description: what is asked and not yet settled — one topic per file, `Q:` / `A: ?` blocks
read_when: "opening a drill topic"
---

# question

What is asked and not yet settled, one topic per file: the body is `Q:` /
`A: ?` blocks under a `----` rule, filled in by [[drill]] one answer at a
time. Trust: none. Once every `A:` is filled and the picture holds, the file
is rewritten in the [[decision]] shape and moves to `decision/`.