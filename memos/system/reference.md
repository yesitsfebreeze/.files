---
kind: type
type: reference
description: a pointer out of the record — a link, a document, a name to look up
read_when: "labelling an external address worth keeping"
---

# reference

An external address worth keeping: a URL, a spec, a ticket, a person to ask.
The claim is the pointer and the reason it is worth following, never a copy
of what it points at — a copy goes stale silently where a pointer does not.