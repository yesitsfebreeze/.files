require: checks.leader_wired.exit equals 0

F1 leader (now the cockpit): tmux.conf parses, every cockpit.tsv row is well-formed under a group, and every WezTerm action it sends is mapped in wezterm.lua.
